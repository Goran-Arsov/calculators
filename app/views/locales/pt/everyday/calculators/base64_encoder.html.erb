<%# SEO meta tags %>
<% set_meta_tags(
  title: "Codificador e Decodificador Base64 - Ferramenta Online Gratuita",
  description: "Codifique e decodifique texto para Base64 online. Suporte a codificação Base64 padrão e segura para URL, com resultados instantâneos e análise do tamanho em bytes.",
  canonical: locale_canonical_url,
  og: {
    title: "Codificador e Decodificador Base64 - Ferramenta Online Gratuita | CalcWise",
    description: "Codifique e decodifique texto para Base64 online. Suporte a codificação Base64 padrão e segura para URL, com resultados instantâneos e análise do tamanho em bytes.",
    url: locale_canonical_url,
    type: "website",
    site_name: "CalcWise"
  }
) %>

<%# JSON-LD schemas %>
<%= breadcrumb_schema([
  { name: "Início", url: root_url },
  { name: "Dia a Dia", url: category_url("everyday") },
  { name: "Codificador e Decodificador Base64", url: everyday_base64_encoder_url }
]) %>

<%= faq_schema([
  { question: "O que é a codificação Base64?", answer: "Base64 é um esquema de codificação de binário para texto que representa dados binários utilizando 64 caracteres ASCII (A-Z, a-z, 0-9, +, /). É comumente usado para incorporar dados binários em formatos baseados em texto como JSON, XML, HTML e e-mail." },
  { question: "Por que o Base64 aumenta o tamanho dos dados?", answer: "O Base64 usa 4 caracteres para representar cada 3 bytes de dados de entrada, resultando em um aumento de tamanho de aproximadamente 33%. Essa compensação é aceita porque o texto codificado é seguro para transmissão por canais somente de texto que poderiam corromper dados binários brutos." },
  { question: "O que é Base64 seguro para URL?", answer: "O Base64 seguro para URL substitui os caracteres + e / por - e _ respectivamente, tornando a string codificada segura para uso em URLs e nomes de arquivo sem codificação percentual adicional. O caractere de preenchimento = também pode ser omitido." },
  { question: "Quando devo usar a codificação Base64?", answer: "Os usos comuns incluem incorporar imagens em CSS ou HTML como URIs de dados, transmitir dados binários por APIs JSON, codificar credenciais de autenticação em cabeçalhos HTTP e armazenar dados binários em bancos de dados ou arquivos de configuração somente de texto." },
  { question: "O Base64 pode ser usado para criptografia?", answer: "Não. O Base64 é um esquema de codificação, não de criptografia. É trivialmente reversível e não fornece segurança. Qualquer pessoa pode decodificar texto Base64 sem uma chave. Para segurança, use algoritmos de criptografia adequados como AES e, opcionalmente, codifique a saída criptografada em Base64 para transporte de texto." }
]) %>

<%= calculator_schema(
  name: "Codificador e Decodificador Base64",
  description: "Codifique e decodifique texto para Base64 com resultados instantâneos e análise do tamanho em bytes.",
  url: everyday_base64_encoder_url,
  category: "UtilityApplication"
) %>

<%# Breadcrumbs %>
<%= render "shared/breadcrumbs", items: [
  { name: "Início", url: root_path },
  { name: "Dia a Dia", url: category_path("everyday") },
  { name: "Codificador e Decodificador Base64" }
] %>

<div class="lg:grid lg:grid-cols-3 lg:gap-8">
  <%= hreflang_tags %>
  <div class="lg:col-span-2">
    <%= render "shared/language_switcher" %>
    <h1 class="text-3xl md:text-4xl font-extrabold text-gray-900 dark:text-white mb-3 tracking-tight">Codificador e Decodificador Base64</h1>
    <p class="text-lg text-gray-500 dark:text-gray-400 mb-8 leading-relaxed">Codifique ou decodifique texto de e para o formato Base64. Veja os comprimentos de caracteres e os tamanhos em bytes da entrada e saída instantaneamente.</p>

    <%# Ad: leaderboard — placed below content heading per AdSense best practices %>
    <div class="mb-8"><%= render "shared/ad_slot", slot: "leaderboard" %></div>

    <div data-controller="base64-encoder-calculator ad-engagement" class="bg-white dark:bg-gray-900 rounded-2xl shadow-sm border border-gray-200/80 dark:border-gray-800 p-6 md:p-8 mb-8">
      <div class="space-y-6">
        <div>
          <label for="base64-encoder-input" class="block text-sm font-semibold text-gray-600 dark:text-gray-400 mb-1.5">Texto de Entrada</label>
          <textarea id="base64-encoder-input" data-base64-encoder-calculator-target="input"
            class="w-full rounded-xl border-[1.5px] border-gray-200 dark:border-gray-600 bg-white dark:bg-gray-700 text-gray-900 dark:text-white p-3 text-sm font-mono focus:ring-2 focus:ring-blue-500 focus:border-blue-500"
            rows="6" placeholder="Digite o texto para codificar ou decodificar..."></textarea>
        </div>

        <div class="flex gap-4">
          <button data-action="click->base64-encoder-calculator#encode"
            class="flex-1 px-4 py-2.5 bg-gradient-to-r from-blue-600 to-indigo-600 hover:from-blue-700 hover:to-indigo-700 text-white rounded-xl shadow-sm shadow-blue-500/20 hover:shadow-md hover:shadow-blue-500/25 transition-all duration-200 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2 text-sm font-medium">Codificar</button>
          <button data-action="click->base64-encoder-calculator#decode"
            class="flex-1 px-4 py-2.5 bg-gradient-to-r from-emerald-600 to-teal-600 hover:from-emerald-700 hover:to-teal-700 text-white rounded-xl shadow-sm shadow-emerald-500/20 hover:shadow-md hover:shadow-emerald-500/25 transition-all duration-200 focus:outline-none focus:ring-2 focus:ring-emerald-500 focus:ring-offset-2 text-sm font-medium">Decodificar</button>
        </div>

        <div class="bg-gray-50 dark:bg-gray-800 rounded-xl p-4 grid grid-cols-4 gap-4">
          <div class="text-center">
            <span class="block text-xs font-semibold text-gray-500 dark:text-gray-400 uppercase tracking-wide mb-1">Comprimento da Entrada</span>
            <span class="text-sm font-bold text-blue-600 dark:text-blue-400" data-base64-encoder-calculator-target="resultInputLength">&mdash;</span>
          </div>
          <div class="text-center">
            <span class="block text-xs font-semibold text-gray-500 dark:text-gray-400 uppercase tracking-wide mb-1">Comprimento da Saída</span>
            <span class="text-sm font-bold text-blue-600 dark:text-blue-400" data-base64-encoder-calculator-target="resultOutputLength">&mdash;</span>
          </div>
          <div class="text-center">
            <span class="block text-xs font-semibold text-gray-500 dark:text-gray-400 uppercase tracking-wide mb-1">Tamanho da Entrada</span>
            <span class="text-sm font-bold text-blue-600 dark:text-blue-400" data-base64-encoder-calculator-target="resultInputBytes">&mdash;</span>
          </div>
          <div class="text-center">
            <span class="block text-xs font-semibold text-gray-500 dark:text-gray-400 uppercase tracking-wide mb-1">Tamanho da Saída</span>
            <span class="text-sm font-bold text-blue-600 dark:text-blue-400" data-base64-encoder-calculator-target="resultOutputBytes">&mdash;</span>
          </div>
        </div>

        <div>
          <div class="flex justify-between items-center mb-1.5">
            <label for="base64-encoder-output" class="text-sm font-semibold text-gray-600 dark:text-gray-400">Saída</label>
            <button data-action="click->base64-encoder-calculator#copy" class="text-xs px-2 py-1 bg-blue-100 dark:bg-blue-900/30 text-blue-600 dark:text-blue-400 rounded-lg hover:bg-blue-200 dark:hover:bg-blue-900/50 transition-colors">Copiar</button>
          </div>
          <textarea id="base64-encoder-output" data-base64-encoder-calculator-target="output" readonly
            class="w-full rounded-xl border-[1.5px] border-gray-200 dark:border-gray-600 bg-gray-50 dark:bg-gray-800 text-gray-900 dark:text-white p-3 text-sm font-mono"
            rows="6" placeholder="O texto codificado ou decodificado aparecerá aqui..."></textarea>
        </div>
      </div>
    </div>

    <%= render "shared/calculator_rating", slug: "base64-encoder" %>
    <div class="mb-8"><%= render "shared/ad_slot", slot: "in_results" %></div>

    <article class="prose dark:prose-invert max-w-none">
      <h2>Como Usar o Codificador e Decodificador Base64</h2>
      <p>
        Cole seu texto na área de entrada e clique em Codificar para convertê-lo para o formato Base64, ou cole texto codificado em Base64 e clique em Decodificar para restaurar o conteúdo original. A ferramenta processa seu texto instantaneamente e exibe o resultado na área de saída. A grade de estatísticas mostra os comprimentos de caracteres e os tamanhos em bytes tanto para a entrada quanto para a saída, facilitando a visualização da sobrecarga de tamanho introduzida pela codificação. Use o botão Copiar para copiar a saída para a área de transferência com um clique.
      </p>
      <p>
        Esta ferramenta é ideal para desenvolvedores que precisam codificar strings rapidamente para requisições de API, decodificar tokens Base64 para depuração ou verificar se os dados codificados fazem a conversão de ida e volta corretamente. Ela lida com texto UTF-8 perfeitamente, então caracteres multibyte como letras acentuadas, emojis e ideogramas CJK são codificados e decodificados sem corrupção.
      </p>

      <div class="my-6"><%= render "shared/ad_slot", slot: "in_content" %></div>

      <h2>O Que É a Codificação Base64 e Como Funciona?</h2>
      <p>
        Base64 é um esquema de codificação de binário para texto que representa dados binários arbitrários utilizando um conjunto de 64 caracteres ASCII imprimíveis: as letras maiúsculas de A a Z, as letras minúsculas de a a z, os dígitos de 0 a 9 e os dois símbolos + e /. Um caractere de preenchimento = é usado quando o comprimento da entrada não é múltiplo de três bytes. O processo de codificação pega cada três bytes de entrada (24 bits) e os divide em quatro grupos de seis bits, cada um dos quais é mapeado para um dos 64 caracteres. Isso produz quatro caracteres de saída para cada três bytes de entrada.
      </p>
      <p>
        O esquema foi originalmente definido para a codificação de e-mail MIME e agora está especificado na RFC 4648. É um dos esquemas de codificação mais amplamente utilizados na web porque transforma qualquer carga binária em uma string segura para inclusão em protocolos e formatos baseados em texto.
      </p>

      <h3>Casos de Uso Comuns</h3>
      <p>
        A codificação Base64 aparece em todo o desenvolvimento web moderno. URIs de dados usam Base64 para incorporar imagens, fontes e outros recursos binários diretamente em HTML e CSS, eliminando requisições HTTP adicionais. A autenticação HTTP Basic codifica o par de nome de usuário e senha como uma string Base64 no cabeçalho Authorization. Os JSON Web Tokens (JWT) usam codificação Base64url para os segmentos de cabeçalho e payload. Anexos de e-mail são codificados com Base64 através do padrão MIME. Payloads de API que precisam transportar dados binários, como uploads de arquivos enviados como JSON, dependem do Base64 para serializar o conteúdo do arquivo em uma string segura para texto.
      </p>

      <h3>Base64 Padrão vs Seguro para URL</h3>
      <p>
        O Base64 padrão usa + e / como o 63o e 64o caracteres, mas esses caracteres têm significados especiais em URLs e nomes de arquivo. O Base64 seguro para URL (também chamado de Base64url, definido na Seção 5 da RFC 4648) substitui + por - e / por _ para evitar conflitos. O caractere de preenchimento = também pode ser omitido nas variantes seguras para URL porque o comprimento original pode ser inferido do comprimento codificado. Tokens JWT, por exemplo, usam Base64url sem preenchimento. Ao trabalhar com APIs web, sempre verifique se o endpoint espera codificação padrão ou segura para URL.
      </p>

      <h3>Sobrecarga de Tamanho Explicada</h3>
      <p>
        Como o Base64 mapeia cada 3 bytes de entrada para 4 caracteres de saída, os dados codificados são sempre aproximadamente 33% maiores que o original. Para um arquivo de 1 KB, a representação em Base64 é de cerca de 1,37 KB. Para uma imagem de 1 MB incorporada como URI de dados, a string codificada é de aproximadamente 1,33 MB. Essa sobrecarga é o custo da representação segura em texto. Quando a largura de banda ou o armazenamento são uma preocupação, considere se o Base64 é realmente necessário ou se um transporte binário como dados de formulário multipart seria mais eficiente.
      </p>

      <h2>Perguntas Frequentes</h2>

      <h3>O que é a codificação Base64?</h3>
      <p>Base64 é um esquema de binário para texto que usa 64 caracteres ASCII (A-Z, a-z, 0-9, +, /). Ele incorpora dados binários com segurança em formatos de texto como JSON, XML, HTML e e-mail.</p>

      <h3>Por que o Base64 aumenta o tamanho dos dados?</h3>
      <p>Ele usa 4 caracteres para cada 3 bytes de entrada, causando um aumento de tamanho de aproximadamente 33%. A compensação é aceita porque a saída é segura para canais somente de texto.</p>

      <h3>O que é Base64 seguro para URL?</h3>
      <p>O Base64 seguro para URL substitui + por - e / por _, tornando as strings codificadas seguras para URLs e nomes de arquivo sem codificação percentual. O preenchimento também pode ser omitido.</p>

      <h3>Quando devo usar a codificação Base64?</h3>
      <p>Para URIs de dados, payloads binários de API JSON, cabeçalhos de autenticação HTTP Basic, tokens JWT, anexos de e-mail e armazenamento de dados binários em arquivos de configuração somente de texto.</p>

      <h3>O Base64 pode ser usado para criptografia?</h3>
      <p>Não. O Base64 é um esquema de codificação, não de criptografia. É trivialmente reversível sem uma chave. Use criptografia adequada como AES primeiro e, opcionalmente, codifique o texto cifrado em Base64.</p>

    <%= render "shared/print_button" %>

    <%# Social sharing %>
    <%= render "shared/social_share", title: "Codificador e Decodificador Base64", url: everyday_base64_encoder_url %>

    <%# Email capture — shows after 2nd calculator visit %>
    <%= render "shared/calculation_email_capture" %>
</article>

    <%= render "shared/related_calculators", related: [
      { name: "Codificador/Decodificador de Strings", description: "Codifique e decodifique texto com Base64, codificação de URL e entidades HTML.", path: everyday_string_encoder_path, icon_path: "M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z" },
      { name: "Codificador/Decodificador de URL", description: "Codifique e decodifique URLs com codificação percentual para uso seguro em endereços web.", path: everyday_url_encoder_path, icon_path: "M13.828 10.172a4 4 0 00-5.656 0l-4 4a4 4 0 105.656 5.656l1.102-1.101m-.758-4.899a4 4 0 005.656 0l4-4a4 4 0 00-5.656-5.656l-1.1 1.1" },
      { name: "Gerador de Hash", description: "Gere hashes MD5, SHA-1, SHA-256 e SHA-512 a partir de texto de entrada.", path: everyday_hash_generator_path, icon_path: "M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z" }
    ] %>

    <%= render "shared/calculator_disclaimer", category: "everyday" %>

    <% blog_posts = related_blog_posts("base64-encoder") %>
    <%= render "shared/recommended_reading", posts: blog_posts if blog_posts.any? %>

  </div>

  <div class="hidden lg:block">
    <div class="sticky top-24"><%= render "shared/ad_slot", slot: "sidebar" %></div>
  </div>
</div>
