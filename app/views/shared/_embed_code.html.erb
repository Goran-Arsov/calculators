<%# Embed code snippet — allows users to embed this calculator on external sites %>
<div class="mt-6 bg-gray-50 dark:bg-gray-900 rounded-2xl border border-gray-200/80 dark:border-gray-800 p-5">
  <div class="flex items-center gap-2 mb-3">
    <svg class="w-5 h-5 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 20l4-16m4 4l4 4-4 4M6 16l-4-4 4-4"/></svg>
    <h3 class="text-sm font-semibold text-gray-700 dark:text-gray-300">Embed This Calculator</h3>
  </div>
  <p class="text-xs text-gray-500 dark:text-gray-400 mb-3">Add this calculator to your website for free.</p>
  <div class="relative">
    <code class="block bg-gray-100 dark:bg-gray-800 rounded-xl p-3 text-xs text-gray-600 dark:text-gray-400 font-mono break-all select-all" id="embed-code">&lt;iframe src="<%= url %>" width="100%" height="500" style="border:none;border-radius:12px;" title="<%= title %>"&gt;&lt;/iframe&gt;</code>
    <button onclick="navigator.clipboard.writeText(document.getElementById('embed-code').textContent);this.textContent='Copied!';setTimeout(()=>this.textContent='Copy',1500)"
            class="absolute top-2 right-2 px-3 py-1 text-xs font-medium bg-white dark:bg-gray-700 text-gray-600 dark:text-gray-300 rounded-lg border border-gray-200 dark:border-gray-600 hover:bg-gray-50 dark:hover:bg-gray-600 transition-colors">
      Copy
    </button>
  </div>
</div>
