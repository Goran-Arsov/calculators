<%# SEO meta tags %>
<% set_meta_tags(
  title: "Eigenvalue Calculator - Find Eigenvalues & Eigenvectors | Calc Hammer",
  description: "Find eigenvalues and eigenvectors of 2x2 and 3x3 matrices. Solves the characteristic polynomial and shows the characteristic equation. Free and instant.",
  canonical: math_eigenvalue_url,
  og: {
    title: "Eigenvalue Calculator | Calc Hammer",
    description: "Find eigenvalues and eigenvectors of 2x2 and 3x3 matrices by solving the characteristic polynomial.",
    url: math_eigenvalue_url,
    type: "website",
    site_name: "Calc Hammer"
  }
) %>

<%= breadcrumb_schema([
  { name: "Home", url: root_url },
  { name: "Math", url: category_url("math") },
  { name: "Eigenvalue Calculator", url: math_eigenvalue_url }
]) %>

<%= faq_schema([
  { question: "What is an eigenvalue?", answer: "An eigenvalue lambda of a square matrix A is a scalar such that Av = lambda*v for some non-zero vector v (the eigenvector). In other words, multiplying A by v simply scales v by lambda without changing its direction. Eigenvalues reveal fundamental properties of linear transformations." },
  { question: "How do you find eigenvalues?", answer: "Eigenvalues are found by solving the characteristic equation det(A - lambda*I) = 0, where I is the identity matrix. For a 2x2 matrix this gives a quadratic equation, and for a 3x3 matrix a cubic equation. The roots of the characteristic polynomial are the eigenvalues." },
  { question: "What are eigenvectors?", answer: "An eigenvector v corresponding to eigenvalue lambda satisfies (A - lambda*I)v = 0. It is found by solving this homogeneous system. The eigenvector represents a direction that is preserved (only scaled) by the linear transformation A." },
  { question: "Can eigenvalues be complex numbers?", answer: "Yes. When the characteristic polynomial has complex roots, the eigenvalues are complex conjugate pairs. This commonly occurs when the matrix represents a rotation. The calculator displays both real and complex eigenvalues." },
  { question: "What is the characteristic polynomial?", answer: "The characteristic polynomial of a matrix A is det(A - lambda*I), a polynomial in lambda whose roots are the eigenvalues. For a 2x2 matrix it is lambda^2 - trace*lambda + det = 0. For a 3x3 matrix it is a cubic polynomial." }
]) %>

<%= calculator_schema_with_ratings(
  name: "Eigenvalue Calculator",
  description: "Find eigenvalues and eigenvectors of 2x2 and 3x3 matrices by solving the characteristic polynomial.",
  url: math_eigenvalue_url,
  category: "MathApplication",
  calculator_slug: "eigenvalue"
) %>

<%= render "shared/breadcrumbs", items: [
  { name: "Home", url: root_path },
  { name: "Math", url: category_path("math") },
  { name: "Eigenvalue Calculator" }
] %>

<div class="lg:grid lg:grid-cols-3 lg:gap-8">
  <div class="lg:col-span-2">
    <h1 class="text-3xl md:text-4xl font-extrabold text-gray-900 dark:text-white mb-3 tracking-tight">Eigenvalue Calculator</h1>
    <p class="text-lg text-gray-500 dark:text-gray-400 mb-8 leading-relaxed">Find eigenvalues and eigenvectors of 2&times;2 and 3&times;3 matrices. Enter the matrix elements and see the characteristic polynomial, eigenvalues, and corresponding eigenvectors instantly.</p>

    <div class="mb-8"><%= render "shared/ad_slot", slot: "leaderboard" %></div>

    <div data-controller="eigenvalue-calculator ad-engagement" class="bg-white dark:bg-gray-900 rounded-2xl shadow-sm border border-gray-200/80 dark:border-gray-800 p-6 md:p-8 mb-8">
      <div class="flex items-center gap-3 mb-5">
        <div class="w-8 h-8 bg-gradient-to-br from-blue-500 to-indigo-600 rounded-lg flex items-center justify-center text-white text-sm font-bold">&lambda;</div>
        <h2 class="text-lg font-semibold text-gray-900 dark:text-white">Find Eigenvalues &amp; Eigenvectors</h2>
      </div>
      <div class="grid grid-cols-1 md:grid-cols-2 gap-6 md:gap-8">
        <div class="space-y-4">
          <div>
            <label class="block text-sm font-semibold text-gray-600 dark:text-gray-400 mb-1.5">Matrix size</label>
            <select data-eigenvalue-calculator-target="size" data-action="change->eigenvalue-calculator#updateVisibility" class="w-full">
              <option value="2">2 &times; 2</option>
              <option value="3">3 &times; 3</option>
            </select>
          </div>
          <div data-eigenvalue-calculator-target="matrix2x2">
            <p class="text-sm font-semibold text-gray-600 dark:text-gray-400 mb-2">Matrix A (2&times;2)</p>
            <div class="grid grid-cols-2 gap-2">
              <input type="number" step="any" value="2" data-eigenvalue-calculator-target="a00" data-action="input->eigenvalue-calculator#calculate" class="w-full text-center font-mono">
              <input type="number" step="any" value="1" data-eigenvalue-calculator-target="a01" data-action="input->eigenvalue-calculator#calculate" class="w-full text-center font-mono">
              <input type="number" step="any" value="1" data-eigenvalue-calculator-target="a10" data-action="input->eigenvalue-calculator#calculate" class="w-full text-center font-mono">
              <input type="number" step="any" value="2" data-eigenvalue-calculator-target="a11" data-action="input->eigenvalue-calculator#calculate" class="w-full text-center font-mono">
            </div>
          </div>
          <div data-eigenvalue-calculator-target="matrix3x3" class="hidden">
            <p class="text-sm font-semibold text-gray-600 dark:text-gray-400 mb-2">Matrix A (3&times;3)</p>
            <div class="grid grid-cols-3 gap-2">
              <input type="number" step="any" value="1" data-eigenvalue-calculator-target="b00" data-action="input->eigenvalue-calculator#calculate" class="w-full text-center font-mono">
              <input type="number" step="any" value="0" data-eigenvalue-calculator-target="b01" data-action="input->eigenvalue-calculator#calculate" class="w-full text-center font-mono">
              <input type="number" step="any" value="0" data-eigenvalue-calculator-target="b02" data-action="input->eigenvalue-calculator#calculate" class="w-full text-center font-mono">
              <input type="number" step="any" value="0" data-eigenvalue-calculator-target="b10" data-action="input->eigenvalue-calculator#calculate" class="w-full text-center font-mono">
              <input type="number" step="any" value="2" data-eigenvalue-calculator-target="b11" data-action="input->eigenvalue-calculator#calculate" class="w-full text-center font-mono">
              <input type="number" step="any" value="0" data-eigenvalue-calculator-target="b12" data-action="input->eigenvalue-calculator#calculate" class="w-full text-center font-mono">
              <input type="number" step="any" value="0" data-eigenvalue-calculator-target="b20" data-action="input->eigenvalue-calculator#calculate" class="w-full text-center font-mono">
              <input type="number" step="any" value="0" data-eigenvalue-calculator-target="b21" data-action="input->eigenvalue-calculator#calculate" class="w-full text-center font-mono">
              <input type="number" step="any" value="3" data-eigenvalue-calculator-target="b22" data-action="input->eigenvalue-calculator#calculate" class="w-full text-center font-mono">
            </div>
          </div>
        </div>
        <div aria-live="polite" aria-atomic="true" class="bg-gradient-to-br from-green-50 to-emerald-50 dark:from-green-900/20 dark:to-emerald-900/20 rounded-2xl p-6">
          <h2 class="text-lg font-semibold text-gray-900 dark:text-white mb-4">Result</h2>
          <div class="space-y-3">
            <div>
              <span class="text-gray-600 dark:text-gray-400 block text-sm mb-1">Eigenvalues</span>
              <span class="text-xl font-bold text-green-600 dark:text-green-400 break-all" data-eigenvalue-calculator-target="result">&mdash;</span>
            </div>
            <div>
              <span class="text-gray-600 dark:text-gray-400 block text-sm mb-1">Eigenvalues &amp; Eigenvectors</span>
              <pre class="text-sm font-mono text-gray-900 dark:text-white whitespace-pre-wrap" data-eigenvalue-calculator-target="resultDetail"></pre>
            </div>
            <div>
              <span class="text-xs text-gray-500 dark:text-gray-400" data-eigenvalue-calculator-target="charPoly"></span>
            </div>
            <div><span class="text-xs text-red-600 dark:text-red-400" data-eigenvalue-calculator-target="error"></span></div>
          </div>
          <button data-action="click->eigenvalue-calculator#copy" class="mt-6 w-full px-4 py-2.5 bg-gradient-to-r from-green-600 to-emerald-600 hover:from-green-700 hover:to-emerald-700 text-white rounded-xl shadow-sm shadow-green-500/20 hover:shadow-md hover:shadow-green-500/25 transition-all duration-200 focus:outline-none focus:ring-2 focus:ring-green-500 focus:ring-offset-2 text-sm font-medium">Copy Result</button>
        </div>
      </div>
    </div>

    <%= render "shared/calculator_rating", slug: "eigenvalue" %>
    <div class="mb-8"><%= render "shared/ad_slot", slot: "in_results" %></div>

    <article class="prose dark:prose-invert max-w-none">
      <h2>How to Use the Eigenvalue Calculator</h2>
      <p>Choose the matrix size (2&times;2 or 3&times;3), enter the matrix elements, and the calculator will solve the characteristic polynomial to find all eigenvalues. For each real eigenvalue, the corresponding eigenvector is computed and displayed. Complex eigenvalues are shown in a + bi form.</p>

      <p>For example, the identity matrix has eigenvalue 1 with multiplicity equal to the matrix size. A rotation matrix will typically produce complex eigenvalues related to the rotation angle.</p>

      <div class="my-6"><%= render "shared/ad_slot", slot: "in_content" %></div>

      <h2>Understanding Eigenvalues and Eigenvectors</h2>
      <p>The eigenvalue equation Av = &lambda;v says that the matrix A, when applied to the eigenvector v, produces a vector in the same direction scaled by &lambda;. Finding eigenvalues reduces to solving the characteristic polynomial det(A &minus; &lambda;I) = 0. For a 2&times;2 matrix, this is a quadratic equation with a closed-form solution. For 3&times;3, it requires solving a cubic.</p>

      <h3>Properties</h3>
      <p>The sum of all eigenvalues equals the trace (sum of diagonal elements), and the product of all eigenvalues equals the determinant. A matrix is invertible if and only if none of its eigenvalues are zero. Symmetric matrices always have real eigenvalues.</p>

      <h3>Applications</h3>
      <p>Eigenvalues and eigenvectors are central to principal component analysis (PCA) in data science, vibration analysis in mechanical engineering, quantum mechanics (observable operators), Google&rsquo;s PageRank algorithm, stability analysis of dynamical systems, and image compression (SVD).</p>

      <h2>Frequently Asked Questions</h2>

      <h3>What is an eigenvalue?</h3>
      <p>A scalar &lambda; such that Av = &lambda;v for some non-zero vector v. It represents a scaling factor for the eigenvector direction.</p>

      <h3>How do you find eigenvalues?</h3>
      <p>Solve det(A &minus; &lambda;I) = 0. For 2&times;2 this is a quadratic, for 3&times;3 a cubic equation.</p>

      <h3>Can eigenvalues be complex?</h3>
      <p>Yes, when the characteristic polynomial has complex roots. This occurs commonly with rotation matrices.</p>

      <h3>What is the characteristic polynomial?</h3>
      <p>det(A &minus; &lambda;I), a polynomial whose roots are the eigenvalues of A.</p>

      <h3>What are eigenvectors used for?</h3>
      <p>PCA, vibration analysis, quantum mechanics, PageRank, stability analysis, and image compression.</p>

      <%= render "shared/print_button" %>
      <%= render "shared/calculation_email_capture" %>
    </article>

    <%= render "shared/related_calculators", related: [
      { name: "Matrix Calculator", description: "Add, subtract, multiply matrices and compute determinants.", path: math_matrix_path, icon_path: "M4 6a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2H6a2 2 0 01-2-2V6zm10 0a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2h-2a2 2 0 01-2-2V6zM4 16a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2H6a2 2 0 01-2-2v-2zm10 0a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2h-2a2 2 0 01-2-2v-2z" },
      { name: "Vector Calculator", description: "Compute dot product, cross product, and magnitude.", path: math_vector_path, icon_path: "M5 12h14m-4-4l4 4-4 4" },
      { name: "Complex Number Calculator", description: "Perform arithmetic on complex numbers.", path: math_complex_number_path, icon_path: "M12 4v16m-8-8h16" }
    ] %>

    <%= render "shared/calculator_disclaimer", category: "math" %>
  </div>

  <div class="hidden lg:block">
    <div class="sticky top-24"><%= render "shared/ad_slot", slot: "sidebar" %></div>
  </div>
</div>
