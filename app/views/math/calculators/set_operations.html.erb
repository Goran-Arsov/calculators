<%# SEO meta tags %>
<% set_meta_tags(
  title: "Set Operations Calculator - Union, Intersection, Power Set | CalcWise",
  description: "Compute union, intersection, difference, symmetric difference, complement, power set, and cardinality. Check subset and disjoint properties. Free online tool.",
  canonical: math_set_operations_url,
  og: {
    title: "Set Operations Calculator | CalcWise",
    description: "Compute union, intersection, difference, symmetric difference, complement, and power set of sets.",
    url: math_set_operations_url,
    type: "website",
    site_name: "CalcWise"
  }
) %>

<%= breadcrumb_schema([
  { name: "Home", url: root_url },
  { name: "Math", url: category_url("math") },
  { name: "Set Operations Calculator", url: math_set_operations_url }
]) %>

<%= faq_schema([
  { question: "What is a set in mathematics?", answer: "A set is a well-defined collection of distinct objects called elements. Sets are denoted with curly braces, e.g., A = {1, 2, 3}. The order of elements does not matter, and duplicates are ignored. Sets are fundamental to discrete mathematics, logic, and computer science." },
  { question: "What is the union of two sets?", answer: "The union of sets A and B (written A U B) is the set of all elements that are in A, in B, or in both. For example, {1, 2, 3} U {3, 4, 5} = {1, 2, 3, 4, 5}. Union combines both sets without duplicates." },
  { question: "What is the intersection of two sets?", answer: "The intersection of sets A and B (written A ∩ B) is the set of elements that are in both A and B. For example, {1, 2, 3} ∩ {3, 4, 5} = {3}. If two sets have no common elements, their intersection is the empty set." },
  { question: "What is a power set?", answer: "The power set P(A) of a set A is the set of all possible subsets of A, including the empty set and A itself. If A has n elements, the power set has 2^n subsets. For example, P({1, 2}) = {{}, {1}, {2}, {1, 2}}." },
  { question: "What is the symmetric difference?", answer: "The symmetric difference of A and B (written A Δ B) is the set of elements in either A or B, but not in both. It equals (A - B) U (B - A). For example, {1, 2, 3} Δ {3, 4, 5} = {1, 2, 4, 5}." }
]) %>

<%= calculator_schema_with_ratings(
  name: "Set Operations Calculator",
  description: "Compute union, intersection, difference, symmetric difference, complement, power set, and cardinality of sets.",
  url: math_set_operations_url,
  category: "MathApplication",
  calculator_slug: "set_operations"
) %>

<%= render "shared/breadcrumbs", items: [
  { name: "Home", url: root_path },
  { name: "Math", url: category_path("math") },
  { name: "Set Operations Calculator" }
] %>

<div class="lg:grid lg:grid-cols-3 lg:gap-8">
  <div class="lg:col-span-2">
    <h1 class="text-3xl md:text-4xl font-extrabold text-gray-900 dark:text-white mb-3 tracking-tight">Set Operations Calculator</h1>
    <p class="text-lg text-gray-500 dark:text-gray-400 mb-8 leading-relaxed">Compute union, intersection, difference, symmetric difference, complement, power set, and cardinality. Enter sets as comma-separated values and see all operations at once.</p>

    <div class="mb-8"><%= render "shared/ad_slot", slot: "leaderboard" %></div>

    <div data-controller="set-operations-calculator ad-engagement" class="bg-white dark:bg-gray-900 rounded-2xl shadow-sm border border-gray-200/80 dark:border-gray-800 p-6 md:p-8 mb-8">
      <div class="flex items-center gap-3 mb-5">
        <div class="w-8 h-8 bg-gradient-to-br from-blue-500 to-indigo-600 rounded-lg flex items-center justify-center text-white text-sm font-bold">{}</div>
        <h2 class="text-lg font-semibold text-gray-900 dark:text-white">Set Operations</h2>
      </div>
      <div class="space-y-4 mb-6">
        <div>
          <label class="block text-sm font-semibold text-gray-600 dark:text-gray-400 mb-1.5">Set A (comma-separated)</label>
          <input type="text" autocomplete="off" data-set-operations-calculator-target="setA" data-action="input->set-operations-calculator#calculate" class="w-full font-mono" placeholder="e.g. 1, 2, 3, 4" value="1, 2, 3, 4">
        </div>
        <div>
          <label class="block text-sm font-semibold text-gray-600 dark:text-gray-400 mb-1.5">Set B (comma-separated)</label>
          <input type="text" autocomplete="off" data-set-operations-calculator-target="setB" data-action="input->set-operations-calculator#calculate" class="w-full font-mono" placeholder="e.g. 3, 4, 5, 6" value="3, 4, 5, 6">
        </div>
        <div>
          <label class="block text-sm font-semibold text-gray-600 dark:text-gray-400 mb-1.5">Universal Set (optional, for complement)</label>
          <input type="text" autocomplete="off" data-set-operations-calculator-target="universalSet" data-action="input->set-operations-calculator#calculate" class="w-full font-mono" placeholder="e.g. 1, 2, 3, 4, 5, 6, 7, 8" value="1, 2, 3, 4, 5, 6, 7, 8">
        </div>
      </div>
      <div aria-live="polite" aria-atomic="true" class="bg-gradient-to-br from-green-50 to-emerald-50 dark:from-green-900/20 dark:to-emerald-900/20 rounded-2xl p-6">
        <h2 class="text-lg font-semibold text-gray-900 dark:text-white mb-4">Results</h2>
        <div class="grid grid-cols-1 sm:grid-cols-2 gap-4 text-sm">
          <div>
            <span class="text-gray-500 dark:text-gray-400 block mb-0.5">A &cup; B (Union)</span>
            <span class="font-semibold font-mono text-gray-900 dark:text-white" data-set-operations-calculator-target="union">&mdash;</span>
          </div>
          <div>
            <span class="text-gray-500 dark:text-gray-400 block mb-0.5">A &cap; B (Intersection)</span>
            <span class="font-semibold font-mono text-gray-900 dark:text-white" data-set-operations-calculator-target="intersection">&mdash;</span>
          </div>
          <div>
            <span class="text-gray-500 dark:text-gray-400 block mb-0.5">A &minus; B (Difference)</span>
            <span class="font-semibold font-mono text-gray-900 dark:text-white" data-set-operations-calculator-target="differenceAB">&mdash;</span>
          </div>
          <div>
            <span class="text-gray-500 dark:text-gray-400 block mb-0.5">B &minus; A (Difference)</span>
            <span class="font-semibold font-mono text-gray-900 dark:text-white" data-set-operations-calculator-target="differenceBA">&mdash;</span>
          </div>
          <div>
            <span class="text-gray-500 dark:text-gray-400 block mb-0.5">A &Delta; B (Symmetric Diff)</span>
            <span class="font-semibold font-mono text-gray-900 dark:text-white" data-set-operations-calculator-target="symmetricDifference">&mdash;</span>
          </div>
          <div>
            <span class="text-gray-500 dark:text-gray-400 block mb-0.5">A' (Complement)</span>
            <span class="font-semibold font-mono text-gray-900 dark:text-white" data-set-operations-calculator-target="complementA">&mdash;</span>
          </div>
          <div>
            <span class="text-gray-500 dark:text-gray-400 block mb-0.5">B' (Complement)</span>
            <span class="font-semibold font-mono text-gray-900 dark:text-white" data-set-operations-calculator-target="complementB">&mdash;</span>
          </div>
          <div>
            <span class="text-gray-500 dark:text-gray-400 block mb-0.5">Cardinality</span>
            <span class="font-semibold text-gray-900 dark:text-white" data-set-operations-calculator-target="cardinalityA">&mdash;</span>
            <span class="font-semibold text-gray-900 dark:text-white ml-2" data-set-operations-calculator-target="cardinalityB">&mdash;</span>
          </div>
          <div class="sm:col-span-2">
            <span class="text-gray-500 dark:text-gray-400 block mb-0.5">P(A) (Power Set)</span>
            <span class="font-mono text-xs text-gray-900 dark:text-white break-all" data-set-operations-calculator-target="powerSetA">&mdash;</span>
            <span class="text-xs text-gray-500 dark:text-gray-400 block mt-0.5" data-set-operations-calculator-target="powerSetSize"></span>
          </div>
          <div>
            <span class="text-gray-500 dark:text-gray-400 block mb-0.5">A &sube; B?</span>
            <span class="font-semibold text-gray-900 dark:text-white" data-set-operations-calculator-target="isSubsetAB">&mdash;</span>
          </div>
          <div>
            <span class="text-gray-500 dark:text-gray-400 block mb-0.5">B &sube; A?</span>
            <span class="font-semibold text-gray-900 dark:text-white" data-set-operations-calculator-target="isSubsetBA">&mdash;</span>
          </div>
          <div>
            <span class="text-gray-500 dark:text-gray-400 block mb-0.5">Disjoint?</span>
            <span class="font-semibold text-gray-900 dark:text-white" data-set-operations-calculator-target="areDisjoint">&mdash;</span>
          </div>
          <div>
            <span class="text-gray-500 dark:text-gray-400 block mb-0.5">A = B?</span>
            <span class="font-semibold text-gray-900 dark:text-white" data-set-operations-calculator-target="areEqual">&mdash;</span>
          </div>
        </div>
        <div class="mt-2"><span class="text-xs text-red-600 dark:text-red-400" data-set-operations-calculator-target="error"></span></div>
        <button data-action="click->set-operations-calculator#copy" class="mt-4 w-full px-4 py-2.5 bg-gradient-to-r from-green-600 to-emerald-600 hover:from-green-700 hover:to-emerald-700 text-white rounded-xl shadow-sm shadow-green-500/20 hover:shadow-md hover:shadow-green-500/25 transition-all duration-200 focus:outline-none focus:ring-2 focus:ring-green-500 focus:ring-offset-2 text-sm font-medium">Copy Results</button>
      </div>
    </div>

    <%= render "shared/calculator_rating", slug: "set_operations" %>
    <div class="mb-8"><%= render "shared/ad_slot", slot: "in_results" %></div>

    <article class="prose dark:prose-invert max-w-none">
      <h2>How to Use the Set Operations Calculator</h2>
      <p>Enter the elements of Set A and Set B as comma-separated values. Optionally, define a Universal Set for complement operations. All operations are computed simultaneously and displayed in a clear grid. The calculator handles both numeric and text elements.</p>

      <p>For example, with A = {1, 2, 3, 4} and B = {3, 4, 5, 6}: Union = {1, 2, 3, 4, 5, 6}, Intersection = {3, 4}, A &minus; B = {1, 2}, Symmetric Difference = {1, 2, 5, 6}.</p>

      <div class="my-6"><%= render "shared/ad_slot", slot: "in_content" %></div>

      <h2>Understanding Set Operations</h2>
      <p>Sets are one of the most fundamental concepts in mathematics. A set is simply a collection of distinct objects. Set operations provide ways to combine, compare, and analyze these collections.</p>

      <h3>Core Operations</h3>
      <p><strong>Union</strong> (A &cup; B) combines all elements from both sets. <strong>Intersection</strong> (A &cap; B) finds common elements. <strong>Difference</strong> (A &minus; B) finds elements in A but not B. <strong>Symmetric difference</strong> (A &Delta; B) finds elements in exactly one set. <strong>Complement</strong> (A') finds elements in the universal set but not in A.</p>

      <h3>Power Set</h3>
      <p>The power set P(A) is the set of all subsets of A. If A has n elements, P(A) has 2<sup>n</sup> subsets. The power set grows exponentially, which is why this calculator limits it to sets with up to 10 elements for display.</p>

      <h3>Applications</h3>
      <p>Set operations are used in database queries (SQL UNION, INTERSECT, EXCEPT), probability theory (event spaces), data analysis (Venn diagram analysis), programming (array operations, data filtering), formal logic, and combinatorics. Understanding sets is fundamental to discrete mathematics and computer science.</p>

      <h2>Frequently Asked Questions</h2>

      <h3>What is a set in mathematics?</h3>
      <p>A collection of distinct objects called elements. Order does not matter and duplicates are ignored.</p>

      <h3>What is the union?</h3>
      <p>A &cup; B contains all elements from either A or B or both.</p>

      <h3>What is the intersection?</h3>
      <p>A &cap; B contains only elements present in both A and B.</p>

      <h3>What is the power set?</h3>
      <p>The set of all subsets of A, including the empty set and A itself. Has 2<sup>n</sup> elements.</p>

      <h3>What is the symmetric difference?</h3>
      <p>A &Delta; B contains elements in either A or B but not both. Equals (A &minus; B) &cup; (B &minus; A).</p>

      <%= render "shared/print_button" %>
      <%= render "shared/calculation_email_capture" %>
    </article>

    <%= render "shared/related_calculators", related: [
      { name: "Boolean Algebra Simplifier", description: "Simplify boolean expressions with truth tables.", path: math_boolean_algebra_path, icon_path: "M9 3v2m6-2v2M9 19v2m6-2v2M5 9H3m2 6H3m18-6h-2m2 6h-2M7 19h10a2 2 0 002-2V7a2 2 0 00-2-2H7a2 2 0 00-2 2v10a2 2 0 002 2z" },
      { name: "Permutation & Combination Calculator", description: "Calculate permutations and combinations.", path: math_permutation_combination_path, icon_path: "M4 8V4m0 0h4M4 4l5 5m11-1V4m0 0h-4m4 0l-5 5M4 16v4m0 0h4m-4 0l5-5m11 5l-5-5m5 5v-4m0 4h-4" },
      { name: "Probability Calculator", description: "Calculate probability from events and outcomes.", path: math_probability_path, icon_path: "M13 10V3L4 14h7v7l9-11h-7z" }
    ] %>

    <%= render "shared/calculator_disclaimer", category: "math" %>
  </div>

  <div class="hidden lg:block">
    <div class="sticky top-24"><%= render "shared/ad_slot", slot: "sidebar" %></div>
  </div>
</div>
