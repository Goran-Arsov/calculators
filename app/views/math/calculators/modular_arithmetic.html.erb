<%# SEO meta tags %>
<% set_meta_tags(
  title: "Modular Arithmetic Calculator - Mod Operations Online | CalcWise",
  description: "Compute modular addition, subtraction, multiplication, exponentiation (fast power), and modular inverse. Free online modular arithmetic calculator.",
  canonical: math_modular_arithmetic_url,
  og: {
    title: "Modular Arithmetic Calculator | CalcWise",
    description: "Compute modular addition, subtraction, multiplication, fast exponentiation, and modular inverse.",
    url: math_modular_arithmetic_url,
    type: "website",
    site_name: "CalcWise"
  }
) %>

<%= breadcrumb_schema([
  { name: "Home", url: root_url },
  { name: "Math", url: category_url("math") },
  { name: "Modular Arithmetic Calculator", url: math_modular_arithmetic_url }
]) %>

<%= faq_schema([
  { question: "What is modular arithmetic?", answer: "Modular arithmetic is a system of arithmetic for integers where numbers wrap around after reaching a certain value called the modulus. We write a mod m to mean the remainder when a is divided by m. For example, 17 mod 5 = 2 because 17 = 3*5 + 2. It is sometimes called clock arithmetic." },
  { question: "What is modular exponentiation?", answer: "Modular exponentiation computes a^b mod m efficiently without computing the full value of a^b (which could be astronomically large). This calculator uses binary exponentiation (also called fast power or square-and-multiply), which runs in O(log b) multiplications. It is fundamental to RSA encryption and other cryptographic algorithms." },
  { question: "What is a modular inverse?", answer: "The modular inverse of a modulo m is a number x such that a*x is congruent to 1 (mod m). It exists if and only if gcd(a, m) = 1 (i.e., a and m are coprime). The calculator finds it using the Extended Euclidean Algorithm." },
  { question: "When does a modular inverse not exist?", answer: "The modular inverse of a mod m does not exist when gcd(a, m) is not equal to 1. For example, 4 has no inverse mod 6 because gcd(4, 6) = 2. The modular inverse always exists when m is prime and a is not a multiple of m." },
  { question: "Where is modular arithmetic used?", answer: "Modular arithmetic is used in cryptography (RSA, Diffie-Hellman), hashing algorithms, check digits (ISBN, credit cards), computer science (circular buffers, hash tables), and number theory. It is also the basis for modular arithmetic in competitive programming." }
]) %>

<%= calculator_schema_with_ratings(
  name: "Modular Arithmetic Calculator",
  description: "Compute modular addition, subtraction, multiplication, exponentiation, and modular inverse.",
  url: math_modular_arithmetic_url,
  category: "MathApplication",
  calculator_slug: "modular_arithmetic"
) %>

<%= render "shared/breadcrumbs", items: [
  { name: "Home", url: root_path },
  { name: "Math", url: category_path("math") },
  { name: "Modular Arithmetic Calculator" }
] %>

<div class="lg:grid lg:grid-cols-3 lg:gap-8">
  <div class="lg:col-span-2">
    <h1 class="text-3xl md:text-4xl font-extrabold text-gray-900 dark:text-white mb-3 tracking-tight">Modular Arithmetic Calculator</h1>
    <p class="text-lg text-gray-500 dark:text-gray-400 mb-8 leading-relaxed">Compute modular addition, subtraction, multiplication, fast exponentiation, and modular inverse. Essential for cryptography, number theory, and competitive programming.</p>

    <div class="mb-8"><%= render "shared/ad_slot", slot: "leaderboard" %></div>

    <div data-controller="modular-arithmetic-calculator ad-engagement" class="bg-white dark:bg-gray-900 rounded-2xl shadow-sm border border-gray-200/80 dark:border-gray-800 p-6 md:p-8 mb-8">
      <div class="flex items-center gap-3 mb-5">
        <div class="w-8 h-8 bg-gradient-to-br from-blue-500 to-indigo-600 rounded-lg flex items-center justify-center text-white text-sm font-bold">%</div>
        <h2 class="text-lg font-semibold text-gray-900 dark:text-white">Modular Operations</h2>
      </div>
      <div class="grid grid-cols-1 md:grid-cols-2 gap-6 md:gap-8">
        <div class="space-y-4">
          <div>
            <label class="block text-sm font-semibold text-gray-600 dark:text-gray-400 mb-1.5">Operation</label>
            <select data-modular-arithmetic-calculator-target="operation" data-action="change->modular-arithmetic-calculator#updateVisibility" class="w-full">
              <option value="add">Add (a + b) mod m</option>
              <option value="subtract">Subtract (a - b) mod m</option>
              <option value="multiply">Multiply (a &times; b) mod m</option>
              <option value="exponentiate">Exponentiate a^b mod m</option>
              <option value="inverse">Inverse a&minus;&sup1; mod m</option>
            </select>
          </div>
          <div>
            <label class="block text-sm font-semibold text-gray-600 dark:text-gray-400 mb-1.5">a</label>
            <input type="number" value="7" data-modular-arithmetic-calculator-target="a" data-action="input->modular-arithmetic-calculator#calculate" class="w-full">
          </div>
          <div data-modular-arithmetic-calculator-target="bInput">
            <label class="block text-sm font-semibold text-gray-600 dark:text-gray-400 mb-1.5">b</label>
            <input type="number" value="3" data-modular-arithmetic-calculator-target="b" data-action="input->modular-arithmetic-calculator#calculate" class="w-full">
          </div>
          <div>
            <label class="block text-sm font-semibold text-gray-600 dark:text-gray-400 mb-1.5">Modulus (m)</label>
            <input type="number" min="2" value="13" data-modular-arithmetic-calculator-target="modulus" data-action="input->modular-arithmetic-calculator#calculate" class="w-full">
          </div>
        </div>
        <div aria-live="polite" aria-atomic="true" class="bg-gradient-to-br from-green-50 to-emerald-50 dark:from-green-900/20 dark:to-emerald-900/20 rounded-2xl p-6">
          <h2 class="text-lg font-semibold text-gray-900 dark:text-white mb-4">Result</h2>
          <div class="space-y-3">
            <div>
              <span class="text-2xl font-bold text-green-600 dark:text-green-400 break-all" data-modular-arithmetic-calculator-target="result">&mdash;</span>
            </div>
            <div>
              <pre class="text-sm text-gray-500 dark:text-gray-400 whitespace-pre-wrap" data-modular-arithmetic-calculator-target="resultDetail"></pre>
            </div>
            <div><span class="text-xs text-red-600 dark:text-red-400" data-modular-arithmetic-calculator-target="error"></span></div>
          </div>
          <button data-action="click->modular-arithmetic-calculator#copy" class="mt-6 w-full px-4 py-2.5 bg-gradient-to-r from-green-600 to-emerald-600 hover:from-green-700 hover:to-emerald-700 text-white rounded-xl shadow-sm shadow-green-500/20 hover:shadow-md hover:shadow-green-500/25 transition-all duration-200 focus:outline-none focus:ring-2 focus:ring-green-500 focus:ring-offset-2 text-sm font-medium">Copy Result</button>
        </div>
      </div>
    </div>

    <%= render "shared/calculator_rating", slug: "modular_arithmetic" %>
    <div class="mb-8"><%= render "shared/ad_slot", slot: "in_results" %></div>

    <article class="prose dark:prose-invert max-w-none">
      <h2>How to Use the Modular Arithmetic Calculator</h2>
      <p>Select the operation, enter the values for a, b, and the modulus m. For modular inverse, only a and m are needed (b is ignored). Results update instantly as you type.</p>

      <p>For modular exponentiation, the calculator uses binary exponentiation (square-and-multiply method), which efficiently computes a<sup>b</sup> mod m even for very large exponents without computing the full power.</p>

      <div class="my-6"><%= render "shared/ad_slot", slot: "in_content" %></div>

      <h2>Modular Arithmetic Explained</h2>
      <p>In modular arithmetic, we work with remainders after division by the modulus. Two integers are <strong>congruent modulo m</strong> if they have the same remainder when divided by m. This creates an arithmetic system where numbers &ldquo;wrap around&rdquo; after reaching the modulus, much like hours on a 12-hour clock.</p>

      <h3>Modular Exponentiation</h3>
      <p>Computing a<sup>b</sup> mod m directly would require computing a<sup>b</sup> first, which could have millions of digits. <strong>Binary exponentiation</strong> avoids this by reducing modulo m at each step: it decomposes the exponent into binary and uses repeated squaring, performing only O(log b) modular multiplications. This is the core algorithm behind RSA encryption.</p>

      <h3>Modular Inverse</h3>
      <p>The modular inverse of a mod m is computed using the <strong>Extended Euclidean Algorithm</strong>, which finds integers x and y such that ax + my = gcd(a, m). If gcd(a, m) = 1, then x mod m is the inverse. The inverse exists only when a and m are coprime.</p>

      <h2>Frequently Asked Questions</h2>

      <h3>What is modular arithmetic?</h3>
      <p>Arithmetic where numbers wrap around after reaching the modulus. a mod m gives the remainder of a divided by m.</p>

      <h3>What is modular exponentiation?</h3>
      <p>Efficiently computing a<sup>b</sup> mod m using binary exponentiation without computing the full power.</p>

      <h3>What is a modular inverse?</h3>
      <p>A number x such that a &times; x &equiv; 1 (mod m). Found using the Extended Euclidean Algorithm.</p>

      <h3>When does the inverse not exist?</h3>
      <p>When gcd(a, m) &ne; 1. It always exists when m is prime and a is not a multiple of m.</p>

      <h3>Where is modular arithmetic used?</h3>
      <p>Cryptography (RSA, Diffie-Hellman), hashing, check digits, competitive programming, and number theory.</p>

      <%= render "shared/print_button" %>
      <%= render "shared/calculation_email_capture" %>
    </article>

    <%= render "shared/related_calculators", related: [
      { name: "GCD & LCM Calculator", description: "Find the greatest common divisor and least common multiple.", path: math_gcd_lcm_path, icon_path: "M4 8V4m0 0h4M4 4l5 5m11-1V4m0 0h-4m4 0l-5 5M4 16v4m0 0h4m-4 0l5-5m11 5l-5-5m5 5v-4m0 4h-4" },
      { name: "Base Arithmetic Calculator", description: "Perform arithmetic in any number base from 2 to 36.", path: math_base_arithmetic_path, icon_path: "M8 7h12m0 0l-4-4m4 4l-4 4m0 6H4m0 0l4 4m-4-4l4-4" },
      { name: "Exponent Calculator", description: "Raise any base to any power.", path: math_exponent_path, icon_path: "M13 10V3L4 14h7v7l9-11h-7z" }
    ] %>

    <%= render "shared/calculator_disclaimer", category: "math" %>
  </div>

  <div class="hidden lg:block">
    <div class="sticky top-24"><%= render "shared/ad_slot", slot: "sidebar" %></div>
  </div>
</div>
