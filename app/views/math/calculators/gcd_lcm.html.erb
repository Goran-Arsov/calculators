<%# SEO meta tags %>
<% set_meta_tags(
  title: "GCD & LCM Calculator - Free Online Tool",
  description: "Calculate the Greatest Common Divisor (GCD) and Least Common Multiple (LCM) of two numbers instantly. Free math tool — no sign-up required.",
  canonical: math_gcd_lcm_url,
  og: {
    title: "GCD & LCM Calculator - Free Online Tool | CalcWise",
    description: "Calculate the Greatest Common Divisor (GCD) and Least Common Multiple (LCM) of two numbers instantly.",
    url: math_gcd_lcm_url,
    type: "website",
    site_name: "CalcWise"
  }
) %>

<%# JSON-LD schemas %>
<%= breadcrumb_schema([
  { name: "Home", url: root_url },
  { name: "Math", url: category_url("math") },
  { name: "GCD & LCM Calculator", url: math_gcd_lcm_url }
]) %>

<%= faq_schema([
  { question: "What is the Greatest Common Divisor (GCD)?", answer: "The GCD of two numbers is the largest positive integer that divides both numbers without leaving a remainder. For example, the GCD of 12 and 18 is 6 because 6 is the largest number that divides evenly into both 12 and 18. The GCD is also called the Greatest Common Factor (GCF) or Highest Common Factor (HCF)." },
  { question: "What is the Least Common Multiple (LCM)?", answer: "The LCM of two numbers is the smallest positive integer that is a multiple of both numbers. For example, the LCM of 4 and 6 is 12 because 12 is the first number that appears in both the multiples of 4 (4, 8, 12, 16...) and the multiples of 6 (6, 12, 18, 24...)." },
  { question: "How are GCD and LCM related?", answer: "The GCD and LCM of two numbers a and b are related by the formula: GCD(a, b) × LCM(a, b) = a × b. This means if you know one, you can calculate the other. For example, GCD(12, 18) = 6 and LCM(12, 18) = 36, and 6 × 36 = 12 × 18 = 216." },
  { question: "How do you calculate GCD using the Euclidean algorithm?", answer: "The Euclidean algorithm finds the GCD by repeatedly dividing the larger number by the smaller one and replacing the larger with the remainder. Continue until the remainder is zero. The last non-zero remainder is the GCD. For example: GCD(48, 18): 48 ÷ 18 = 2 remainder 12, then 18 ÷ 12 = 1 remainder 6, then 12 ÷ 6 = 2 remainder 0, so GCD = 6." },
  { question: "When do you use GCD and LCM in real life?", answer: "GCD is used to simplify fractions (divide numerator and denominator by their GCD), distribute items into equal groups, and tile floors with the largest possible square tile. LCM is used to find common denominators when adding fractions, schedule recurring events, and synchronise cycles like gear rotations or traffic lights." }
]) %>

<%= calculator_schema(
  name: "GCD & LCM Calculator",
  description: "Calculate the Greatest Common Divisor and Least Common Multiple of two numbers.",
  url: math_gcd_lcm_url,
  category: "MathApplication"
) %>

<%# Breadcrumbs %>
<%= render "shared/breadcrumbs", items: [
  { name: "Home", url: root_path },
  { name: "Math", url: category_path("math") },
  { name: "GCD & LCM Calculator" }
] %>

<div class="lg:grid lg:grid-cols-3 lg:gap-8">
  <div class="lg:col-span-2">
    <div class="mb-8"><%= render "shared/ad_slot", slot: "leaderboard" %></div>

    <h1 class="text-3xl md:text-4xl font-extrabold text-gray-900 dark:text-white mb-3 tracking-tight">GCD & LCM Calculator</h1>
    <p class="text-lg text-gray-500 dark:text-gray-400 mb-8 leading-relaxed">Enter two numbers to find their Greatest Common Divisor (GCD) and Least Common Multiple (LCM) instantly.</p>

    <%# Calculator UI %>
    <div data-controller="gcd-lcm-calculator" class="bg-white dark:bg-gray-900 rounded-2xl shadow-sm border border-gray-200/80 dark:border-gray-800 p-6 md:p-8 mb-8">
      <div class="flex items-center gap-3 mb-5">
        <div class="w-8 h-8 bg-gradient-to-br from-blue-500 to-indigo-600 rounded-lg flex items-center justify-center text-white text-xs font-bold">G</div>
        <h2 class="text-lg font-semibold text-gray-900 dark:text-white">Find GCD & LCM</h2>
        <span class="text-sm text-gray-400 dark:text-gray-500 ml-auto font-mono">GCD &times; LCM = a &times; b</span>
      </div>
      <div class="grid grid-cols-1 md:grid-cols-2 gap-6 md:gap-8">
        <div class="space-y-4">
          <div>
            <label class="block text-sm font-semibold text-gray-600 dark:text-gray-400 mb-1.5">First Number</label>
            <input type="number" data-gcd-lcm-calculator-target="a" data-action="input->gcd-lcm-calculator#calculate" class="w-full" placeholder="48" step="1" min="1">
          </div>
          <div>
            <label class="block text-sm font-semibold text-gray-600 dark:text-gray-400 mb-1.5">Second Number</label>
            <input type="number" data-gcd-lcm-calculator-target="b" data-action="input->gcd-lcm-calculator#calculate" class="w-full" placeholder="18" step="1" min="1">
          </div>
        </div>
        <div class="bg-gradient-to-br from-blue-50 to-indigo-50 dark:from-blue-900/20 dark:to-indigo-900/20 rounded-2xl p-6">
          <h2 class="text-lg font-semibold text-gray-900 dark:text-white mb-4">Results</h2>
          <div class="space-y-3">
            <div class="flex justify-between items-center">
              <span class="text-gray-600 dark:text-gray-400">GCD</span>
              <span class="text-xl font-bold text-blue-600 dark:text-blue-400" data-gcd-lcm-calculator-target="gcd">&mdash;</span>
            </div>
            <div class="flex justify-between items-center">
              <span class="text-gray-600 dark:text-gray-400">LCM</span>
              <span class="text-xl font-bold text-blue-600 dark:text-blue-400" data-gcd-lcm-calculator-target="lcm">&mdash;</span>
            </div>
          </div>
          <button data-action="click->gcd-lcm-calculator#copy" class="mt-6 w-full px-4 py-2.5 bg-gradient-to-r from-blue-600 to-indigo-600 hover:from-blue-700 hover:to-indigo-700 text-white rounded-xl shadow-sm shadow-blue-500/20 hover:shadow-md hover:shadow-blue-500/25 transition-all duration-200 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2 text-sm font-medium">Copy Results</button>
        </div>
      </div>
    </div>

    <div class="mb-8"><%= render "shared/ad_slot", slot: "in_results" %></div>

    <article class="prose dark:prose-invert max-w-none">
      <h2>How to Use the GCD & LCM Calculator</h2>
      <p>Enter two positive integers into the input fields, and the calculator instantly displays both the Greatest Common Divisor (GCD) and the Least Common Multiple (LCM). Results update in real time as you type, with no button to press or page to reload. Use this tool whenever you need to simplify fractions, find common denominators, or solve number theory problems.</p>

      <p>The GCD and LCM are fundamental concepts in arithmetic and number theory. They appear in fraction operations, ratio simplification, scheduling problems, and even cryptography. Understanding how they relate to each other gives you a powerful toolkit for working with whole numbers efficiently.</p>

      <div class="my-6"><%= render "shared/ad_slot", slot: "in_content" %></div>

      <h2>How GCD and LCM Are Calculated</h2>
      <p>The most efficient way to find the GCD is the <strong>Euclidean algorithm</strong>, which dates back over 2,000 years. It works by repeatedly replacing the larger number with the remainder of dividing the two numbers until one of them reaches zero. The last non-zero value is the GCD. For example, to find GCD(48, 18): 48 mod 18 = 12, then 18 mod 12 = 6, then 12 mod 6 = 0, so GCD = 6.</p>
      <p>Once you know the GCD, the LCM is calculated using the relationship <strong>LCM(a, b) = (a &times; b) / GCD(a, b)</strong>. Using the same example: LCM(48, 18) = (48 &times; 18) / 6 = 144. This formula is far more efficient than listing multiples of both numbers.</p>

      <h3>Why GCD and LCM Matter</h3>
      <p>The GCD is essential for <strong>simplifying fractions</strong>. Dividing both the numerator and denominator by their GCD produces the fraction in its lowest terms. The LCM is essential for <strong>adding and subtracting fractions</strong> because it gives you the least common denominator. Beyond fractions, the LCM helps synchronise repeating events. If one bus arrives every 12 minutes and another every 18 minutes, they will both arrive together every LCM(12, 18) = 36 minutes.</p>

      <h3>Real-World Applications</h3>
      <p>Carpenters use the GCD to cut boards into the largest equal pieces without waste. Event planners use the LCM to find when two recurring events coincide. Programmers use the GCD in cryptographic algorithms like RSA, where working with coprime numbers is critical. In music theory, the LCM determines when two rhythmic patterns played simultaneously will realign to their starting position.</p>

      <h2>Frequently Asked Questions</h2>

      <h3>What is the Greatest Common Divisor (GCD)?</h3>
      <p>The GCD of two numbers is the largest positive integer that divides both numbers without leaving a remainder. For example, the GCD of 12 and 18 is 6 because 6 is the largest number that divides evenly into both 12 and 18. The GCD is also called the Greatest Common Factor (GCF) or Highest Common Factor (HCF).</p>

      <h3>What is the Least Common Multiple (LCM)?</h3>
      <p>The LCM of two numbers is the smallest positive integer that is a multiple of both numbers. For example, the LCM of 4 and 6 is 12 because 12 is the first number that appears in both the multiples of 4 and the multiples of 6.</p>

      <h3>How are GCD and LCM related?</h3>
      <p>The GCD and LCM of two numbers a and b are related by the formula: GCD(a, b) &times; LCM(a, b) = a &times; b. This means if you know one, you can calculate the other.</p>

      <h3>How do you calculate GCD using the Euclidean algorithm?</h3>
      <p>The Euclidean algorithm finds the GCD by repeatedly dividing the larger number by the smaller one and replacing the larger with the remainder. Continue until the remainder is zero. The last non-zero remainder is the GCD. For example: GCD(48, 18) proceeds 48 &rarr; 12 &rarr; 6 &rarr; 0, so GCD = 6.</p>

      <h3>When do you use GCD and LCM in real life?</h3>
      <p>GCD is used to simplify fractions, distribute items into equal groups, and tile floors with the largest possible square tile. LCM is used to find common denominators when adding fractions, schedule recurring events, and synchronise cycles like gear rotations or traffic lights.</p>
    </article>

    <%= render "shared/related_calculators", related: [

      { name: "Fraction Calculator", description: "Add, subtract, multiply, and divide fractions with step-by-step results.", path: math_fraction_path, icon_path: "M9 7h6m0 10v-3m-3 3h.01M9 17h.01M9 14h.01M12 14h.01M15 11h.01M12 11h.01M9 11h.01M7 21h10a2 2 0 002-2V5a2 2 0 00-2-2H7a2 2 0 00-2 2v14a2 2 0 002 2z" },
      { name: "Percentage Calculator", description: "Find percentages, percentage change, and more.", path: math_percentage_path, icon_path: "M9 7h6m0 10v-3m-3 3h.01M9 17h.01M9 14h.01M12 14h.01M15 11h.01M12 11h.01M9 11h.01M7 21h10a2 2 0 002-2V5a2 2 0 00-2-2H7a2 2 0 00-2 2v14a2 2 0 002 2z" },
      { name: "Exponent Calculator", description: "Raise any base to any power and get the result instantly.", path: math_exponent_path, icon_path: "M4 7h16M4 12h8m-8 5h16" }
    ] %>

    <%# Calculator disclaimer %>
    <%= render "shared/calculator_disclaimer" %>
  </div>

  <div class="hidden lg:block">
    <div class="sticky top-24"><%= render "shared/ad_slot", slot: "sidebar" %></div>
  </div>
</div>
