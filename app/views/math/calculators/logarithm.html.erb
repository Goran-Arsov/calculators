<%# SEO meta tags %>
<% set_meta_tags(
  title: "Logarithm Calculator - Free Online Tool",
  description: "Calculate logarithms with any base instantly. Supports natural log (ln), log base 10, log base 2, and custom bases — no sign-up required.",
  canonical: math_logarithm_url,
  og: {
    title: "Logarithm Calculator - Free Online Tool | CalcWise",
    description: "Calculate logarithms with any base instantly. Supports natural log, log10, log2, and custom bases.",
    url: math_logarithm_url,
    type: "website",
    site_name: "CalcWise"
  }
) %>

<%# JSON-LD schemas %>
<%= breadcrumb_schema([
  { name: "Home", url: root_url },
  { name: "Math", url: category_url("math") },
  { name: "Logarithm Calculator", url: math_logarithm_url }
]) %>

<%= faq_schema([
  { question: "What is a logarithm?", answer: "A logarithm answers the question: to what power must a given base be raised to produce a certain number? If b^x = y, then log_b(y) = x. For example, log_2(8) = 3 because 2^3 = 8. Logarithms are the inverse of exponentiation." },
  { question: "What is the difference between ln, log, and log2?", answer: "ln is the natural logarithm with base e (approximately 2.71828). log usually refers to the common logarithm with base 10. log2 is the binary logarithm with base 2. All three are related by the change-of-base formula: log_b(x) = ln(x) / ln(b)." },
  { question: "Why can't you take the logarithm of a negative number?", answer: "In real number mathematics, logarithms are only defined for positive numbers. This is because no real exponent can make a positive base produce a negative result. In complex analysis, logarithms of negative numbers do exist but involve imaginary numbers." },
  { question: "What is the change of base formula?", answer: "The change of base formula lets you convert a logarithm from one base to another: log_b(x) = log_a(x) / log_a(b). This is useful because most calculators only have ln and log10 buttons. For example, log_5(25) = ln(25) / ln(5) = 2." },
  { question: "Where are logarithms used in real life?", answer: "Logarithms are used in measuring earthquake intensity (Richter scale), sound levels (decibels), pH in chemistry, radioactive decay, compound interest calculations, information theory (bits), and algorithm complexity analysis in computer science." }
]) %>

<%= calculator_schema(
  name: "Logarithm Calculator",
  description: "Calculate logarithms with any base including natural log (ln), log base 10, log base 2, and custom bases.",
  url: math_logarithm_url,
  category: "EducationalApplication"
) %>

<%# Breadcrumbs %>
<%= render "shared/breadcrumbs", items: [
  { name: "Home", url: root_path },
  { name: "Math", url: category_path("math") },
  { name: "Logarithm Calculator" }
] %>

<div class="lg:grid lg:grid-cols-3 lg:gap-8">
  <div class="lg:col-span-2">
    <div class="mb-8"><%= render "shared/ad_slot", slot: "leaderboard" %></div>

    <h1 class="text-3xl md:text-4xl font-extrabold text-gray-900 dark:text-white mb-3 tracking-tight">Logarithm Calculator</h1>
    <p class="text-lg text-gray-500 dark:text-gray-400 mb-8 leading-relaxed">Enter a value and base to compute logarithms instantly. See ln, log<sub>10</sub>, log<sub>2</sub>, and custom base results side by side.</p>

    <%# Calculator UI %>
    <div data-controller="logarithm-calculator ad-engagement" class="bg-white dark:bg-gray-900 rounded-2xl shadow-sm border border-gray-200/80 dark:border-gray-800 p-6 md:p-8 mb-8">
      <div class="flex items-center gap-3 mb-5">
        <div class="w-8 h-8 bg-gradient-to-br from-blue-500 to-indigo-600 rounded-lg flex items-center justify-center text-white text-xs font-bold">log</div>
        <h2 class="text-lg font-semibold text-gray-900 dark:text-white">Compute Logarithm</h2>
        <span class="text-sm text-gray-400 dark:text-gray-500 ml-auto font-mono">log<sub>b</sub>(x)</span>
      </div>
      <div class="grid grid-cols-1 md:grid-cols-2 gap-6 md:gap-8">
        <div class="space-y-4">
          <div>
            <label for="logarithm-value" class="block text-sm font-semibold text-gray-600 dark:text-gray-400 mb-1.5">Value (x)</label>
            <input id="logarithm-value" type="number" data-logarithm-calculator-target="value" data-action="input->logarithm-calculator#calculate" class="w-full" placeholder="100" step="any" min="0.0001">
          </div>
          <div>
            <label for="logarithm-base-e-for-ln-10-2-or-any-positive-number" class="block text-sm font-semibold text-gray-600 dark:text-gray-400 mb-1.5">Base (b) <span class="font-normal text-gray-400">e for ln, 10, 2, or any positive number &ne; 1</span></label>
            <input id="logarithm-base-e-for-ln-10-2-or-any-positive-number" type="text" data-logarithm-calculator-target="base" data-action="input->logarithm-calculator#calculate" class="w-full" placeholder="e" value="e">
          </div>
        </div>
        <div aria-live="polite" class="bg-gradient-to-br from-blue-50 to-indigo-50 dark:from-blue-900/20 dark:to-indigo-900/20 rounded-2xl p-6">
          <h2 class="text-lg font-semibold text-gray-900 dark:text-white mb-4">Results</h2>
          <div class="space-y-3">
            <div class="flex justify-between items-center">
              <span class="text-gray-600 dark:text-gray-400">log<sub>b</sub>(x)</span>
              <span class="text-xl font-bold text-blue-600 dark:text-blue-400" data-logarithm-calculator-target="result">&mdash;</span>
            </div>
            <div class="flex justify-between items-center">
              <span class="text-gray-600 dark:text-gray-400">ln(x)</span>
              <span class="text-xl font-bold text-blue-600 dark:text-blue-400" data-logarithm-calculator-target="ln">&mdash;</span>
            </div>
            <div class="flex justify-between items-center">
              <span class="text-gray-600 dark:text-gray-400">log<sub>10</sub>(x)</span>
              <span class="text-xl font-bold text-blue-600 dark:text-blue-400" data-logarithm-calculator-target="log10">&mdash;</span>
            </div>
            <div class="flex justify-between items-center">
              <span class="text-gray-600 dark:text-gray-400">log<sub>2</sub>(x)</span>
              <span class="text-xl font-bold text-blue-600 dark:text-blue-400" data-logarithm-calculator-target="log2">&mdash;</span>
            </div>
          </div>
          <button data-action="click->logarithm-calculator#copy" class="mt-6 w-full px-4 py-2.5 bg-gradient-to-r from-blue-600 to-indigo-600 hover:from-blue-700 hover:to-indigo-700 text-white rounded-xl shadow-sm shadow-blue-500/20 hover:shadow-md hover:shadow-blue-500/25 transition-all duration-200 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2 text-sm font-medium">Copy Results</button>
        </div>
      </div>
    </div>

    <div class="mb-8"><%= render "shared/ad_slot", slot: "in_results" %></div>

    <article class="prose dark:prose-invert max-w-none">
      <h2>How to Use the Logarithm Calculator</h2>
      <p>Enter a positive number in the value field and choose your base. Type <code>e</code> for the natural logarithm, <code>10</code> for the common logarithm, or any positive number other than 1 for a custom base. The calculator displays the result for your chosen base alongside ln, log<sub>10</sub>, and log<sub>2</sub> for easy comparison.</p>

      <p>Logarithms are the inverse of exponentiation and appear throughout science, engineering, and finance. Understanding them unlocks concepts from pH scales and decibels to compound interest and algorithm complexity. This tool computes all common logarithmic forms simultaneously so you can see the relationships at a glance.</p>

      <div class="my-6"><%= render "shared/ad_slot", slot: "in_content" %></div>

      <h2>Understanding Logarithms</h2>
      <p>If b<sup>x</sup> = y, then log<sub>b</sub>(y) = x. The base b must be positive and not equal to 1. The <strong>natural logarithm</strong> (ln) uses base e &asymp; 2.71828, which arises naturally in calculus and exponential growth. The <strong>common logarithm</strong> (log<sub>10</sub>) is used in orders of magnitude, decibels, and the Richter scale.</p>

      <h3>The Change of Base Formula</h3>
      <p>You can convert between any two bases using <strong>log<sub>b</sub>(x) = ln(x) / ln(b)</strong>. This is exactly how the calculator computes custom-base logarithms. For instance, log<sub>5</sub>(125) = ln(125) / ln(5) = 3, because 5<sup>3</sup> = 125.</p>

      <h3>Key Logarithm Properties</h3>
      <p>Logarithms follow three fundamental rules: <strong>log(xy) = log(x) + log(y)</strong>, <strong>log(x/y) = log(x) &minus; log(y)</strong>, and <strong>log(x<sup>n</sup>) = n &middot; log(x)</strong>. These properties simplify multiplication into addition, which is why logarithms were historically used in slide rules and log tables for computation.</p>

      <h3>Applications of Logarithms</h3>
      <p>Logarithms measure earthquake intensity on the Richter scale (each whole number is a tenfold increase), sound levels in decibels, and acidity on the pH scale. In finance, they model compound interest and continuous growth. In computer science, logarithmic time complexity describes efficient algorithms like binary search. Information theory uses log<sub>2</sub> to measure entropy in bits.</p>

      <h2>Frequently Asked Questions</h2>

      <h3>What is a logarithm?</h3>
      <p>A logarithm answers the question: to what power must a given base be raised to produce a certain number? If b<sup>x</sup> = y, then log<sub>b</sub>(y) = x.</p>

      <h3>What is the difference between ln, log, and log2?</h3>
      <p>ln uses base e (~2.718), log typically means base 10, and log2 uses base 2. They are all related by the change-of-base formula.</p>

      <h3>Why can't you take the logarithm of a negative number?</h3>
      <p>In real mathematics, logarithms are only defined for positive numbers because no real exponent of a positive base can produce a negative result.</p>

      <h3>What is the change of base formula?</h3>
      <p>log<sub>b</sub>(x) = log<sub>a</sub>(x) / log<sub>a</sub>(b). This lets you convert any logarithm to a more convenient base.</p>

      <h3>Where are logarithms used in real life?</h3>
      <p>Earthquake measurement, sound levels, pH scales, radioactive decay, compound interest, information theory, and algorithm analysis all rely on logarithms.</p>

    <%= render "shared/print_button" %>

    <%# Social sharing %>
    <%= render "shared/social_share", title: "Logarithm Calculator", url: math_logarithm_url %>

    <%# Email capture — shows after 2nd calculator visit %>
    <%= render "shared/calculation_email_capture" %>

      <div class="my-6"><%= render "shared/ad_slot", slot: "after_intro" %></div>

    </article>

    <%= render "shared/related_calculators", related: [
      { name: "Exponent Calculator", description: "Raise any base to any power and get the result instantly.", path: math_exponent_path, icon_path: "M13 10V3L4 14h7v7l9-11h-7z" },
      { name: "Percentage Calculator", description: "Calculate percentages, percentage change, and more.", path: math_percentage_path, icon_path: "M7 20l4-16m2 16l4-16M6 9h14M4 15h14" },
      { name: "Standard Deviation Calculator", description: "Calculate mean, standard deviation, and variance from a set of numbers.", path: math_standard_deviation_path, icon_path: "M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z" }
    ] %>

    <%# Calculator disclaimer %>
    <%= render "shared/calculator_disclaimer", category: "math" %>
  </div>

  <div class="hidden lg:block">
    <div class="sticky top-24"><%= render "shared/ad_slot", slot: "sidebar" %></div>
  </div>
</div>
