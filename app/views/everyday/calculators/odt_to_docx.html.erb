<%# SEO meta tags %>
<% set_meta_tags(
  title: "ODT to DOCX Converter - Free Online Tool",
  description: "Convert OpenDocument .odt files to Microsoft Word .docx format instantly in your browser. Upload a document, preview the extracted text and headings, and download a DOCX file. No data leaves your device.",
  canonical: everyday_odt_to_docx_url,
  og: {
    title: "ODT to DOCX Converter - Free Online Tool | CalcWise",
    description: "Convert OpenDocument .odt files to Word .docx format entirely in your browser. No data leaves your device.",
    url: everyday_odt_to_docx_url,
    type: "website",
    site_name: "CalcWise"
  }
) %>

<%# JSON-LD schemas %>
<%= breadcrumb_schema([
  { name: "Home", url: root_url },
  { name: "Everyday", url: category_url("everyday") },
  { name: "ODT to DOCX Converter", url: everyday_odt_to_docx_url }
]) %>

<%= faq_schema([
  { question: "How does this ODT to DOCX converter work?", answer: "The tool reads your .odt file entirely in the browser using JavaScript. An ODT file is a ZIP archive containing XML files following the OpenDocument format. The tool decompresses the archive, locates the main content file (content.xml), parses the paragraph and heading structure, and builds a minimal DOCX file containing Word-compatible XML. No data is uploaded to any server." },
  { question: "Will my document be uploaded to a server?", answer: "No. The entire conversion happens locally in your browser. Your document content never leaves your device. You can verify this by disconnecting from the internet before converting — the tool works completely offline." },
  { question: "What formatting is preserved in the DOCX output?", answer: "The tool extracts text content and heading levels (Heading 1 through Heading 6) from your ODT file. These are mapped to the equivalent Word heading styles in the DOCX output. Lists are converted to bulleted text paragraphs. Complex formatting such as images, tables, custom fonts, colors, and page layout settings are not preserved." },
  { question: "Can I convert the DOCX back to ODT?", answer: "This tool performs one-way conversion from ODT to DOCX. To convert back, you can open the DOCX file in LibreOffice or OpenOffice and save it as ODT. The text content and heading structure will be preserved in both directions." },
  { question: "What applications create ODT files?", answer: "ODT (OpenDocument Text) files are created by LibreOffice Writer, Apache OpenOffice Writer, Google Docs (as an export option), and other applications that support the OASIS OpenDocument format. It is the default document format for LibreOffice, which is the most widely used free office suite." }
]) %>

<%= calculator_schema(
  name: "ODT to DOCX Converter",
  description: "Convert OpenDocument .odt files to Microsoft Word .docx format directly in the browser.",
  url: everyday_odt_to_docx_url,
  category: "UtilityApplication"
) %>

<%# Breadcrumbs %>
<%= render "shared/breadcrumbs", items: [
  { name: "Home", url: root_path },
  { name: "Everyday", url: category_path("everyday") },
  { name: "ODT to DOCX Converter" }
] %>

<div class="lg:grid lg:grid-cols-3 lg:gap-8">
  <div class="lg:col-span-2">
    <h1 class="text-3xl md:text-4xl font-extrabold text-gray-900 dark:text-white mb-3 tracking-tight">ODT to DOCX Converter</h1>
    <p class="text-lg text-gray-500 dark:text-gray-400 mb-8 leading-relaxed">Upload an OpenDocument .odt file to convert it to Microsoft Word .docx format. Preview the extracted text and headings, then download the DOCX file. Everything runs locally in your browser.</p>

    <%# Ad: leaderboard — placed below content heading per AdSense best practices %>
    <div class="mb-8"><%= render "shared/ad_slot", slot: "leaderboard" %></div>

    <div data-controller="odt-to-docx-calculator ad-engagement" class="bg-white dark:bg-gray-900 rounded-2xl shadow-sm border border-gray-200/80 dark:border-gray-800 p-6 md:p-8 mb-8">
      <div class="space-y-5">
        <%# File upload %>
        <div>
          <label for="odt-docx-file" class="block text-sm font-semibold text-gray-600 dark:text-gray-400 mb-1.5">Upload ODT File (.odt)</label>
          <input id="odt-docx-file" type="file" accept=".odt"
                 data-odt-to-docx-calculator-target="fileInput"
                 data-action="change->odt-to-docx-calculator#loadFile"
                 class="w-full text-sm text-gray-500 file:mr-4 file:py-2 file:px-4 file:rounded-lg file:border-0 file:text-sm file:font-semibold file:bg-blue-50 file:text-blue-700 hover:file:bg-blue-100 dark:file:bg-blue-900/30 dark:file:text-blue-400">
        </div>

        <%# Text preview %>
        <div data-odt-to-docx-calculator-target="preview" class="hidden">
          <h3 class="text-sm font-semibold text-gray-600 dark:text-gray-400 mb-2">Document Preview</h3>
          <div class="overflow-y-auto max-h-96 border border-gray-200 dark:border-gray-700 rounded-xl p-4 bg-gray-50 dark:bg-gray-800 text-sm text-gray-800 dark:text-gray-200" data-odt-to-docx-calculator-target="previewContent"></div>
        </div>

        <%# Stats and download %>
        <div class="flex flex-wrap items-center gap-6">
          <div class="flex gap-6 text-sm">
            <div><span class="text-gray-500 dark:text-gray-400">Paragraphs:</span> <span class="font-semibold text-gray-900 dark:text-white" data-odt-to-docx-calculator-target="resultParagraphs">--</span></div>
            <div><span class="text-gray-500 dark:text-gray-400">Words:</span> <span class="font-semibold text-gray-900 dark:text-white" data-odt-to-docx-calculator-target="resultWords">--</span></div>
          </div>
          <button data-odt-to-docx-calculator-target="downloadBtn" data-action="click->odt-to-docx-calculator#download" disabled
                  class="px-5 py-2.5 bg-gradient-to-r from-green-600 to-emerald-600 hover:from-green-700 hover:to-emerald-700 disabled:from-gray-400 disabled:to-gray-400 text-white rounded-xl shadow-sm transition-all duration-200 focus:outline-none focus:ring-2 focus:ring-green-500 focus:ring-offset-2 text-sm font-medium">
            Download .docx
          </button>
        </div>
      </div>
    </div>

    <%= render "shared/calculator_rating", slug: "odt-to-docx" %>
    <div class="mb-8"><%= render "shared/ad_slot", slot: "in_results" %></div>

    <article class="prose dark:prose-invert max-w-none">
      <h2>How to Convert ODT to DOCX</h2>
      <p>
        Click the file input and select an .odt document from your computer. The tool reads the file entirely within your browser, decompresses the ZIP container, and parses the OpenDocument XML structure from the content.xml file inside. It extracts text content along with heading levels and list items, then displays a preview so you can verify the content. Click "Download .docx" to generate a valid Word document and save it to your device.
      </p>
      <p>
        The DOCX output maps OpenDocument elements to their Word equivalents. Headings defined with <code>&lt;text:h&gt;</code> elements in the ODT become proper Word heading styles (Heading 1 through Heading 6). Regular paragraphs from <code>&lt;text:p&gt;</code> elements become standard body text. List items are converted to bulleted paragraphs. The resulting DOCX file opens correctly in Microsoft Word, Google Docs, and LibreOffice.
      </p>

      <div class="my-6"><%= render "shared/ad_slot", slot: "in_content" %></div>

      <h2>Understanding the ODT Format</h2>
      <p>
        The ODT (OpenDocument Text) format is an open standard for word processing documents developed by OASIS (Organization for the Advancement of Structured Information Standards). It is the default file format for LibreOffice Writer and Apache OpenOffice Writer, two of the most widely used free and open-source office suites. The format is also supported by Google Docs as an export option and by numerous other document editors across all major operating systems.
      </p>
      <p>
        Like the DOCX format used by Microsoft Word, an ODT file is actually a ZIP archive containing multiple XML files. The main content lives in <code>content.xml</code>, which uses the OpenDocument XML namespace (<code>urn:oasis:names:tc:opendocument:xmlns:text:1.0</code>). Paragraphs are wrapped in <code>&lt;text:p&gt;</code> elements, headings in <code>&lt;text:h&gt;</code> elements with an <code>outline-level</code> attribute specifying the heading level, and lists in <code>&lt;text:list&gt;</code> elements containing <code>&lt;text:list-item&gt;</code> children.
      </p>

      <h3>Why Convert ODT to DOCX</h3>
      <p>
        While the ODT format is an open standard, Microsoft Word remains the dominant word processor in corporate and academic environments. When you need to share a document with colleagues, clients, or professors who use Microsoft Office, sending an ODT file can cause compatibility issues. Some recipients may not have software that opens ODT files, or their version of Word may render the document with formatting inconsistencies. Converting to DOCX ensures the document opens natively in Microsoft Word without requiring any additional software or import steps.
      </p>
      <p>
        DOCX conversion is also important for email attachments in professional contexts, document collaboration through Microsoft 365 or SharePoint, submitting assignments through learning management systems that expect Word format, and meeting submission requirements for publishers, government agencies, or legal firms that specify DOCX as the accepted format.
      </p>

      <h3>What This Tool Preserves</h3>
      <p>
        The converter focuses on the textual content and document structure. It extracts plain text, heading hierarchy (levels 1 through 6), and list items from the ODT file. These elements are mapped to their DOCX equivalents: OpenDocument headings become Word heading styles, body paragraphs become normal text, and list items become bulleted paragraphs. This covers documents that are primarily text-based, such as reports, essays, letters, proposals, and articles.
      </p>

      <h3>Limitations</h3>
      <p>
        This is a text-focused converter designed for structural content. Images, charts, and embedded objects stored as separate files within the ODT archive are not transferred to the DOCX output. Tables, custom fonts, text colors, paragraph alignment, headers, footers, page numbers, and footnotes are also not carried over. For documents that rely heavily on visual formatting, opening the ODT file directly in LibreOffice and using its built-in "Save As" DOCX option will produce a more complete conversion.
      </p>

      <h2>Frequently Asked Questions</h2>

      <h3>How does this ODT to DOCX converter work?</h3>
      <p>
        The tool reads the .odt file as a ZIP archive in your browser, decompresses the XML files inside, parses the document structure from content.xml, extracts text and heading levels, and builds a minimal DOCX file with equivalent Word XML structure. No data is sent to any server.
      </p>

      <h3>Will my document be uploaded to a server?</h3>
      <p>
        No. Everything happens locally in your browser. Your document content never leaves your device. The tool works fully offline.
      </p>

      <h3>What formatting is preserved?</h3>
      <p>
        Text content, heading levels (Heading 1-6), and list items are preserved. Complex formatting like images, tables, custom fonts, and colors are not carried over.
      </p>

      <h3>Can I convert the DOCX back to ODT?</h3>
      <p>
        This tool performs one-way conversion. To convert back, open the DOCX file in LibreOffice and save it as ODT.
      </p>

      <h3>What applications create ODT files?</h3>
      <p>
        ODT files are created by LibreOffice Writer, Apache OpenOffice Writer, Google Docs (export option), and other applications supporting the OpenDocument format.
      </p>

    <%= render "shared/print_button" %>
    <%= render "shared/social_share", title: "ODT to DOCX Converter", url: everyday_odt_to_docx_url %>
    <%= render "shared/calculation_email_capture" %>

      <div class="my-6"><%= render "shared/ad_slot", slot: "after_intro" %></div>
    </article>

    <%= render "shared/related_calculators", related: [
      { name: "DOCX to PDF Converter", description: "Convert Word documents to PDF format.", path: everyday_docx_to_pdf_path, icon_path: "M7 21h10a2 2 0 002-2V9.414a1 1 0 00-.293-.707l-5.414-5.414A1 1 0 0012.586 3H7a2 2 0 00-2 2v14a2 2 0 002 2z" },
      { name: "ODT to PDF Converter", description: "Convert OpenDocument files to PDF format.", path: everyday_odt_to_pdf_path, icon_path: "M7 21h10a2 2 0 002-2V9.414a1 1 0 00-.293-.707l-5.414-5.414A1 1 0 0012.586 3H7a2 2 0 00-2 2v14a2 2 0 002 2z" },
      { name: "Markdown to HTML Converter", description: "Convert Markdown syntax to clean HTML.", path: everyday_markdown_to_html_path, icon_path: "M10 20l4-16m4 4l4 4-4 4M6 16l-4-4 4-4" }
    ] %>

    <%= render "shared/calculator_disclaimer", category: "everyday" %>
    <% blog_posts = related_blog_posts("odt-to-docx-converter") %>
    <%= render "shared/recommended_reading", posts: blog_posts if blog_posts.any? %>
  </div>

  <div class="hidden lg:block">
    <div class="sticky top-24"><%= render "shared/ad_slot", slot: "sidebar" %></div>
  </div>
</div>
