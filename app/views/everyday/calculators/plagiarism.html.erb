<%# SEO meta tags %>
<% set_meta_tags(
  title: "Plagiarism Calculator - Free Text Similarity Checker",
  description: "Compare two texts for similarity using 3-gram Jaccard analysis. Detect matching phrases and calculate similarity percentage. Free plagiarism checker for students.",
  canonical: everyday_plagiarism_url,
  og: {
    title: "Plagiarism Calculator - Free Text Similarity Checker | CalcWise",
    description: "Compare two texts for similarity using 3-gram analysis. Detect matching phrases and calculate similarity percentage instantly.",
    url: everyday_plagiarism_url,
    type: "website",
    site_name: "CalcWise"
  }
) %>

<%# JSON-LD schemas %>
<%= breadcrumb_schema([
  { name: "Home", url: root_url },
  { name: "Everyday", url: category_url("everyday") },
  { name: "Plagiarism Calculator", url: everyday_plagiarism_url }
]) %>

<%= faq_schema([
  { question: "How does the plagiarism calculator work?", answer: "The calculator normalizes both texts by converting to lowercase and removing punctuation, then generates 3-grams (sequences of 3 consecutive words) from each text. It uses the Jaccard similarity coefficient to calculate the percentage of matching 3-grams relative to the total unique 3-grams across both texts." },
  { question: "What is a 3-gram in text analysis?", answer: "A 3-gram (or trigram) is a sequence of three consecutive words from a text. For example, 'the quick brown fox' produces the 3-grams: 'the quick brown' and 'quick brown fox'. Comparing 3-grams catches phrase-level similarities that single-word comparison would miss." },
  { question: "What similarity percentage indicates plagiarism?", answer: "There is no universal threshold, but generally: below 15% is typically acceptable and may reflect common phrasing, 15-40% suggests moderate overlap that should be reviewed, and above 40% indicates significant similarity that likely requires attention. Always consider context, as technical writing and quoted material naturally have higher overlap." },
  { question: "Does this tool detect paraphrasing?", answer: "This tool detects exact phrase matches using 3-gram comparison. It will catch copied or lightly modified text, but heavily paraphrased content with different word choices may not be detected. For comprehensive plagiarism detection, consider using this alongside other analysis methods." },
  { question: "Is my text stored or shared when I use this tool?", answer: "No. All processing happens entirely in your browser using JavaScript. Your text is never sent to any server, stored, or shared. You can verify this by using the tool offline after the page loads." }
]) %>

<%= calculator_schema_with_ratings(
  name: "Plagiarism Calculator",
  description: "Compare two texts for similarity using 3-gram Jaccard analysis to detect matching phrases and calculate similarity percentage.",
  url: everyday_plagiarism_url,
  category: "EducationalApplication",
  calculator_slug: "plagiarism"
) %>

<%# Breadcrumbs %>
<%= render "shared/breadcrumbs", items: [
  { name: "Home", url: root_path },
  { name: "Everyday", url: category_path("everyday") },
  { name: "Plagiarism Calculator" }
] %>

<div class="lg:grid lg:grid-cols-3 lg:gap-8">
  <div class="lg:col-span-2">
    <h1 class="text-3xl md:text-4xl font-extrabold text-gray-900 dark:text-white mb-3 tracking-tight">Plagiarism Calculator</h1>
    <p class="text-lg text-gray-500 dark:text-gray-400 mb-4 leading-relaxed">Paste two texts to compare their similarity using 3-gram phrase matching and Jaccard analysis.</p>
    <div class="mb-8 px-4 py-3 bg-amber-50 dark:bg-amber-900/20 border border-amber-200 dark:border-amber-800/50 rounded-xl text-sm text-amber-800 dark:text-amber-300">
      <strong>How it works:</strong> This tool compares the two texts you paste by breaking them into 3-word phrases and measuring overlap. It detects direct copy-paste and close matches, but will not catch paraphrasing or synonym substitution. Maximum 10,000 words per text. This is a quick similarity check — not a replacement for professional plagiarism detection services like Turnitin.
    </div>

    <%# Ad: leaderboard %>
    <div class="mb-8"><%= render "shared/ad_slot", slot: "leaderboard" %></div>

    <div data-controller="plagiarism-calculator ad-engagement" class="bg-white dark:bg-gray-900 rounded-2xl shadow-sm border border-gray-200/80 dark:border-gray-800 p-6 md:p-8 mb-8">
      <div class="space-y-6">
        <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
          <div>
            <label for="plag-text1" class="block text-sm font-semibold text-gray-600 dark:text-gray-400 mb-1.5">Text 1</label>
            <textarea id="plag-text1" data-plagiarism-calculator-target="text1" data-action="input->plagiarism-calculator#calculate"
              class="w-full rounded-xl border-[1.5px] border-gray-200 dark:border-gray-600 bg-white dark:bg-gray-700 text-gray-900 dark:text-white p-3 text-sm focus:ring-2 focus:ring-blue-500 focus:border-blue-500"
              rows="8" placeholder="Paste the first text here..."></textarea>
          </div>
          <div>
            <label for="plag-text2" class="block text-sm font-semibold text-gray-600 dark:text-gray-400 mb-1.5">Text 2</label>
            <textarea id="plag-text2" data-plagiarism-calculator-target="text2" data-action="input->plagiarism-calculator#calculate"
              class="w-full rounded-xl border-[1.5px] border-gray-200 dark:border-gray-600 bg-white dark:bg-gray-700 text-gray-900 dark:text-white p-3 text-sm focus:ring-2 focus:ring-blue-500 focus:border-blue-500"
              rows="8" placeholder="Paste the second text here..."></textarea>
          </div>
        </div>

        <%# Similarity bar %>
        <div>
          <span class="block text-sm font-semibold text-gray-600 dark:text-gray-400 mb-2">Similarity</span>
          <div class="w-full bg-gray-200 dark:bg-gray-700 rounded-full h-4 overflow-hidden">
            <div data-plagiarism-calculator-target="similarityBar" class="h-full rounded-full bg-green-500 transition-all duration-300" style="width: 0%"></div>
          </div>
        </div>

        <div class="bg-gray-50 dark:bg-gray-800 rounded-xl p-4 grid grid-cols-2 md:grid-cols-4 gap-4">
          <div class="text-center">
            <span class="block text-xs font-semibold text-gray-500 dark:text-gray-400 uppercase tracking-wide mb-1">Similarity</span>
            <span class="text-lg font-bold text-blue-600 dark:text-blue-400" data-plagiarism-calculator-target="resultSimilarity">&mdash;</span>
          </div>
          <div class="text-center">
            <span class="block text-xs font-semibold text-gray-500 dark:text-gray-400 uppercase tracking-wide mb-1">Matching Phrases</span>
            <span class="text-lg font-bold text-blue-600 dark:text-blue-400" data-plagiarism-calculator-target="resultMatchingPhrases">&mdash;</span>
          </div>
          <div class="text-center">
            <span class="block text-xs font-semibold text-gray-500 dark:text-gray-400 uppercase tracking-wide mb-1">Phrases (Text 1)</span>
            <span class="text-lg font-bold text-gray-600 dark:text-gray-400" data-plagiarism-calculator-target="resultTotalPhrases1">&mdash;</span>
          </div>
          <div class="text-center">
            <span class="block text-xs font-semibold text-gray-500 dark:text-gray-400 uppercase tracking-wide mb-1">Phrases (Text 2)</span>
            <span class="text-lg font-bold text-gray-600 dark:text-gray-400" data-plagiarism-calculator-target="resultTotalPhrases2">&mdash;</span>
          </div>
        </div>

        <button data-action="click->plagiarism-calculator#copy" class="w-full px-4 py-2.5 bg-gradient-to-r from-blue-600 to-indigo-600 hover:from-blue-700 hover:to-indigo-700 text-white rounded-xl shadow-sm shadow-blue-500/20 hover:shadow-md hover:shadow-blue-500/25 transition-all duration-200 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2 text-sm font-medium">Copy Results</button>
      </div>
    </div>

    <%= render "shared/calculator_rating", slug: "plagiarism" %>
    <div class="mb-8"><%= render "shared/ad_slot", slot: "in_results" %></div>

    <article class="prose dark:prose-invert max-w-none">
      <h2>How to Use the Plagiarism Calculator</h2>
      <p>
        Paste the first text in the left textarea and the second text in the right textarea. The calculator instantly analyzes both texts by breaking them into 3-grams (sequences of three consecutive words) and computing the Jaccard similarity coefficient. The results show the similarity percentage, the number of matching phrases, and the total unique phrases in each text.
      </p>
      <p>
        This tool is designed for students who want to check their work against source material before submitting assignments. It helps identify passages that may need additional paraphrasing or proper citation. All processing happens in your browser, so your text is never sent to any server.
      </p>

      <div class="my-6"><%= render "shared/ad_slot", slot: "in_content" %></div>

      <h2>Understanding the Jaccard Similarity Coefficient</h2>
      <p>
        The Jaccard similarity coefficient measures the overlap between two sets. In this tool, it calculates the ratio of shared 3-grams to the total unique 3-grams across both texts. A score of 0% means no matching phrases, while 100% means the texts share identical phrasing. The 3-gram approach is effective because it catches phrase-level copying that single-word frequency analysis would miss, while remaining resilient to minor word substitutions.
      </p>

      <h3>Interpreting Your Results</h3>
      <p>
        A similarity below 15% typically indicates original writing with common phrases. Between 15% and 40% suggests moderate overlap that may warrant review, especially if the matching phrases come from a specific source rather than general academic language. Above 40% indicates significant textual similarity and likely requires substantial revision or proper attribution. Technical and scientific writing naturally uses more standardized phrasing, so higher baseline similarity is expected in those fields.
      </p>

      <h3>Tips for Reducing Similarity</h3>
      <p>
        Read the source material, then write your understanding in your own words without looking at the original. Use synonyms and restructure sentences, but ensure the meaning remains accurate. Direct quotes should be enclosed in quotation marks with proper citations. Focus on synthesizing ideas from multiple sources rather than relying heavily on any single source. After writing, use this tool to compare your text against the source and revise any passages that still show high similarity.
      </p>

      <h2>Frequently Asked Questions</h2>

      <h3>How does the plagiarism calculator work?</h3>
      <p>
        The calculator normalizes both texts by converting to lowercase and removing punctuation, then generates 3-grams from each text. It uses the Jaccard similarity coefficient to calculate the percentage of matching 3-grams relative to the total unique 3-grams across both texts.
      </p>

      <h3>What is a 3-gram in text analysis?</h3>
      <p>
        A 3-gram is a sequence of three consecutive words from a text. For example, "the quick brown fox" produces the 3-grams: "the quick brown" and "quick brown fox". Comparing 3-grams catches phrase-level similarities that single-word comparison would miss.
      </p>

      <h3>What similarity percentage indicates plagiarism?</h3>
      <p>
        There is no universal threshold. Below 15% is typically acceptable, 15-40% suggests moderate overlap worth reviewing, and above 40% indicates significant similarity requiring attention. Context matters: technical writing naturally has higher overlap.
      </p>

      <h3>Does this tool detect paraphrasing?</h3>
      <p>
        This tool detects exact phrase matches using 3-gram comparison. Heavily paraphrased content with entirely different word choices may not be detected. Use this alongside other analysis methods for comprehensive checking.
      </p>

      <h3>Is my text stored or shared?</h3>
      <p>
        No. All processing happens in your browser using JavaScript. Your text is never sent to any server, stored, or shared.
      </p>

    <%= render "shared/print_button" %>
    <%# Email capture %>
    <%= render "shared/calculation_email_capture" %>
</article>

    <%= render "shared/related_calculators", related: [
      { name: "Text Diff Checker", description: "Compare two texts line by line with diff highlighting.", path: everyday_text_diff_path, icon_path: "M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z" },
      { name: "Word Counter", description: "Count words, characters, sentences, and paragraphs in your text.", path: everyday_word_counter_path, icon_path: "M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z" },
      { name: "Words Per Minute", description: "Measure your typing or reading speed in words per minute.", path: everyday_words_per_minute_path, icon_path: "M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z" }
    ] %>

    <%= render "shared/calculator_disclaimer" %>

    <% blog_posts = related_blog_posts("plagiarism-calculator") %>
    <%= render "shared/recommended_reading", posts: blog_posts if blog_posts.any? %>

  </div>

  <div class="hidden lg:block">
    <div class="sticky top-24"><%= render "shared/ad_slot", slot: "sidebar" %></div>
  </div>
</div>
