<%# SEO meta tags %>
<% set_meta_tags(
  title: "Online Alarm Timer - Free Countdown with Sound Alert",
  description: "Set a countdown timer with loud alarm sound and screen flash when time expires. Enter hours, minutes, seconds and optional reason.",
  canonical: everyday_alarm_timer_url,
  og: {
    title: "Online Alarm Timer - Free Countdown with Sound Alert | CalcWise",
    description: "Set a countdown timer with loud alarm sound and screen flash when time expires.",
    url: everyday_alarm_timer_url,
    type: "website",
    site_name: "CalcWise"
  }
) %>

<%# JSON-LD schemas %>
<%= breadcrumb_schema([
  { name: "Home", url: root_url },
  { name: "Everyday", url: category_url("everyday") },
  { name: "Alarm Timer", url: everyday_alarm_timer_url }
]) %>

<%= faq_schema([
  { question: "How do I set a countdown alarm?", answer: "Enter the desired hours, minutes, and seconds in the input fields, optionally add a reason for the alarm, then click Start. The countdown will begin immediately and a loud alarm with screen flash will trigger when the timer reaches zero." },
  { question: "Will the alarm sound if I switch tabs?", answer: "Yes. The alarm uses the Web Audio API which continues to play even when the browser tab is in the background. The page title also updates with the remaining time so you can see the countdown in your tab bar. A browser notification will appear if you have granted notification permission." },
  { question: "Can I pause and resume the timer?", answer: "Yes. Click the Pause button to pause the countdown at any time. The remaining time is preserved and you can click Resume to continue from where you left off. Click Reset to clear the timer entirely and start over." },
  { question: "What happens when the alarm goes off?", answer: "When the countdown reaches zero, the screen flashes red and white, a loud beeping alarm sound plays, and the status changes to ALARM. If you entered a reason, it is displayed on the alarm screen. Click Stop Alarm or the on-screen button to silence everything." },
  { question: "Does this work on mobile devices?", answer: "Yes. The alarm timer works on all modern browsers including mobile. On iOS, sound may require an initial user interaction before the audio can play. The screen flash and visual alerts work on all devices." }
]) %>

<%= calculator_schema_with_ratings(
  name: "Online Alarm Timer",
  description: "Set a countdown timer with alarm sound and screen flash notification when time expires.",
  url: everyday_alarm_timer_url,
  category: "UtilitiesApplication",
  calculator_slug: "alarm-timer"
) %>

<%# Breadcrumbs %>
<%= render "shared/breadcrumbs", items: [
  { name: "Home", url: root_path },
  { name: "Everyday", url: category_path("everyday") },
  { name: "Alarm Timer" }
] %>

<div class="lg:grid lg:grid-cols-3 lg:gap-8">
  <div class="lg:col-span-2">
    <h1 class="text-3xl md:text-4xl font-extrabold text-gray-900 dark:text-white mb-3 tracking-tight">Online Alarm Timer</h1>
    <p class="text-lg text-gray-500 dark:text-gray-400 mb-8 leading-relaxed">Set a countdown timer with loud alarm sound and full-screen flash when time expires.</p>

    <%# Ad: leaderboard — placed below content heading per AdSense best practices %>
    <div class="mb-8"><%= render "shared/ad_slot", slot: "leaderboard" %></div>

    <div data-controller="alarm-timer-calculator ad-engagement" class="bg-white dark:bg-gray-900 rounded-2xl shadow-sm border border-gray-200/80 dark:border-gray-800 p-6 md:p-8 mb-8">

      <%# Progress bar (100 bars stacked vertically, each full width) %>
      <div class="mb-4">
        <div data-alarm-timer-calculator-target="progressContainer" class="flex flex-col-reverse gap-px w-full rounded-xl overflow-hidden"></div>
        <p data-alarm-timer-calculator-target="progressLabel" class="text-base text-gray-900 dark:text-gray-100 mt-1.5 text-center font-semibold"></p>
      </div>

      <%# Countdown display below progress %>
      <div class="text-center mb-8">
        <div data-alarm-timer-calculator-target="display" class="text-7xl md:text-8xl font-mono font-bold text-gray-900 dark:text-white tracking-wider select-none">00:00:00</div>
        <p data-alarm-timer-calculator-target="status" class="mt-3 text-sm font-semibold text-gray-500 dark:text-gray-400">Ready</p>
      </div>

      <%# Time inputs %>
      <div class="grid grid-cols-3 gap-4 max-w-md mx-auto mb-4">
        <div class="text-center">
          <label for="alarm-hours" class="block text-xs font-semibold text-gray-500 dark:text-gray-400 mb-1 uppercase tracking-wide">Hours</label>
          <input id="alarm-hours" type="number" inputmode="decimal" min="0" max="23" placeholder="0" data-alarm-timer-calculator-target="hours" class="w-full text-center text-2xl font-mono">
        </div>
        <div class="text-center">
          <label for="alarm-minutes" class="block text-xs font-semibold text-gray-500 dark:text-gray-400 mb-1 uppercase tracking-wide">Minutes</label>
          <input id="alarm-minutes" type="number" inputmode="decimal" min="0" max="59" placeholder="0" data-alarm-timer-calculator-target="minutes" class="w-full text-center text-2xl font-mono">
        </div>
        <div class="text-center">
          <label for="alarm-seconds" class="block text-xs font-semibold text-gray-500 dark:text-gray-400 mb-1 uppercase tracking-wide">Seconds</label>
          <input id="alarm-seconds" type="number" inputmode="decimal" min="0" max="59" placeholder="0" data-alarm-timer-calculator-target="seconds" class="w-full text-center text-2xl font-mono">
        </div>
      </div>

      <%# Reason input %>
      <div class="max-w-md mx-auto mb-6">
        <label for="alarm-reason" class="block text-xs font-semibold text-gray-500 dark:text-gray-400 mb-1 uppercase tracking-wide">Alarm Reason (optional)</label>
        <input id="alarm-reason" type="text" data-alarm-timer-calculator-target="reason" placeholder="e.g., Take a break, Meeting starts, Pizza is ready" class="w-full text-sm">
      </div>

      <%# Control buttons %>
      <div class="flex items-center justify-center gap-3 max-w-md mx-auto">
        <button data-alarm-timer-calculator-target="startBtn" data-action="click->alarm-timer-calculator#start" class="flex-1 px-6 py-3 bg-gradient-to-r from-green-600 to-emerald-600 hover:from-green-700 hover:to-emerald-700 text-white rounded-xl shadow-sm shadow-green-500/20 hover:shadow-md hover:shadow-green-500/25 transition-all duration-200 focus:outline-none focus:ring-2 focus:ring-green-500 focus:ring-offset-2 text-sm font-semibold">Start</button>
        <button data-alarm-timer-calculator-target="pauseBtn" data-action="click->alarm-timer-calculator#pause" class="hidden flex-1 px-6 py-3 bg-gradient-to-r from-yellow-500 to-amber-500 hover:from-yellow-600 hover:to-amber-600 text-white rounded-xl shadow-sm shadow-yellow-500/20 hover:shadow-md hover:shadow-yellow-500/25 transition-all duration-200 focus:outline-none focus:ring-2 focus:ring-yellow-500 focus:ring-offset-2 text-sm font-semibold">Pause</button>
        <button data-alarm-timer-calculator-target="resetBtn" data-action="click->alarm-timer-calculator#reset" class="flex-1 px-6 py-3 bg-gradient-to-r from-gray-500 to-gray-600 hover:from-gray-600 hover:to-gray-700 text-white rounded-xl shadow-sm shadow-gray-500/20 hover:shadow-md hover:shadow-gray-500/25 transition-all duration-200 focus:outline-none focus:ring-2 focus:ring-gray-500 focus:ring-offset-2 text-sm font-semibold">Reset</button>
        <button data-alarm-timer-calculator-target="stopAlarmBtn" data-action="click->alarm-timer-calculator#stopAlarm" class="hidden flex-1 px-6 py-3 bg-gradient-to-r from-red-600 to-rose-600 hover:from-red-700 hover:to-rose-700 text-white rounded-xl shadow-sm shadow-red-500/20 hover:shadow-md hover:shadow-red-500/25 transition-all duration-200 focus:outline-none focus:ring-2 focus:ring-red-500 focus:ring-offset-2 text-sm font-semibold">Stop Alarm</button>
      </div>

      <%# Alarm overlay (hidden by default) %>
      <div data-alarm-timer-calculator-target="alarmOverlay" class="hidden fixed inset-0 z-50 flex flex-col items-center justify-center">
        <p class="text-6xl md:text-8xl font-extrabold text-white mb-4 drop-shadow-lg">ALARM!</p>
        <p data-alarm-timer-calculator-target="reasonDisplay" class="text-2xl md:text-3xl text-white mb-8 drop-shadow-md"></p>
        <button data-action="click->alarm-timer-calculator#stopAlarm" class="px-8 py-4 bg-white text-red-600 rounded-2xl text-xl font-bold shadow-2xl hover:bg-gray-100 transition-colors duration-200">Stop Alarm</button>
      </div>
    </div>

    <%= render "shared/calculator_rating", slug: "alarm-timer" %>
    <div class="mb-8"><%= render "shared/ad_slot", slot: "in_results" %></div>

    <article class="prose dark:prose-invert max-w-none">
      <h2>How to Use the Online Alarm Timer</h2>
      <p>
        Enter the desired countdown duration using the hours, minutes, and seconds fields. You can set any combination, from a quick 10-second reminder to a full 23-hour 59-minute 59-second marathon countdown. Optionally, add a reason for your alarm so you remember why you set it when it goes off. Click Start to begin the countdown, and the large display will tick down second by second. When the timer reaches zero, a loud alarm sound plays and the entire screen flashes red and white to grab your attention.
      </p>
      <p>
        You can pause the countdown at any time and resume from where you left off. The Reset button clears everything and returns the timer to its initial state. If you have granted browser notification permissions, a notification will also appear when the alarm fires, which is especially useful if you have switched to another tab or application.
      </p>

      <div class="my-6"><%= render "shared/ad_slot", slot: "in_content" %></div>

      <h2>Productivity Techniques Using Timers</h2>
      <p>
        One of the most popular time management methods is the Pomodoro Technique, developed by Francesco Cirillo in the late 1980s. The method is simple: set a timer for 25 minutes and work with focused concentration until the alarm sounds, then take a 5-minute break. After completing four work intervals, take a longer break of 15 to 30 minutes. This approach combats mental fatigue by ensuring regular breaks and creates a sense of urgency that helps maintain focus throughout each interval.
      </p>

      <h3>Cooking and Kitchen Timers</h3>
      <p>
        A countdown timer is indispensable in the kitchen. Whether you are boiling eggs (6 minutes for soft, 12 for hard), baking bread, steeping tea (3 to 5 minutes depending on type), or grilling steaks, precise timing makes the difference between a perfectly cooked dish and a burnt one. The alarm reason field is particularly handy in the kitchen because you can label each timer with what you are cooking, eliminating confusion when multiple things are in the oven at once.
      </p>

      <h3>Meeting Reminders and Time Boxing</h3>
      <p>
        Time boxing is a scheduling strategy where you allocate a fixed period to each activity. Set the alarm timer before starting a task and commit to finishing or pausing when the alarm sounds. This prevents any single task from consuming your entire day and encourages you to estimate how long things actually take. Over time, you build a more accurate sense of task duration, which improves your planning and reduces the stress of missed deadlines.
      </p>

      <h3>Exercise and Interval Training</h3>
      <p>
        Use the alarm timer for exercise intervals, rest periods between sets, or timed stretching routines. High-intensity interval training (HIIT) relies on alternating bursts of intense activity with recovery periods. Set a timer for your work interval, rest when it sounds, then set another timer for the next round. The loud alarm and screen flash ensure you never miss a transition, even in a noisy gym environment.
      </p>

      <h2>Frequently Asked Questions</h2>

      <h3>How do I set a countdown alarm?</h3>
      <p>
        Enter the desired hours, minutes, and seconds in the input fields, optionally add a reason for the alarm, then click Start. The countdown will begin immediately and a loud alarm with screen flash will trigger when the timer reaches zero.
      </p>

      <h3>Will the alarm sound if I switch tabs?</h3>
      <p>
        Yes. The alarm uses the Web Audio API which continues to play even when the browser tab is in the background. The page title also updates with the remaining time so you can see the countdown in your tab bar. A browser notification will appear if you have granted notification permission.
      </p>

      <h3>Can I pause and resume the timer?</h3>
      <p>
        Yes. Click the Pause button to pause the countdown at any time. The remaining time is preserved and you can click Resume to continue from where you left off. Click Reset to clear the timer entirely and start over.
      </p>

      <h3>What happens when the alarm goes off?</h3>
      <p>
        When the countdown reaches zero, the screen flashes red and white, a loud beeping alarm sound plays, and the status changes to ALARM. If you entered a reason, it is displayed on the alarm screen. Click Stop Alarm to silence everything.
      </p>

      <h3>Does this work on mobile devices?</h3>
      <p>
        Yes. The alarm timer works on all modern browsers including mobile. On iOS, sound may require an initial user interaction before the audio can play. The screen flash and visual alerts work on all devices.
      </p>

    <%= render "shared/print_button" %>

    <%# Social sharing %>
    <%= render "shared/social_share", title: "Online Alarm Timer", url: everyday_alarm_timer_url %>

    <%# Email capture %>
    <%= render "shared/calculation_email_capture" %>
</article>

    <%= render "shared/related_calculators", related: [
      { name: "Alarm Clock", description: "Set an alarm clock with custom sounds and snooze options.", path: everyday_alarm_clock_path, icon_path: "M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z" },
      { name: "Stopwatch", description: "Precise stopwatch with lap timing and split tracking.", path: everyday_stopwatch_path, icon_path: "M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z" },
      { name: "Pomodoro Timer", description: "Focus timer using the Pomodoro Technique with work and break intervals.", path: everyday_pomodoro_timer_path, icon_path: "M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z" }
    ] %>

    <%# Calculator disclaimer %>
    <%= render "shared/calculator_disclaimer", category: "everyday" %>

    <%# Recommended reading %>
    <% blog_posts = related_blog_posts("alarm-timer") %>
    <%= render "shared/recommended_reading", posts: blog_posts if blog_posts.any? %>

  </div>

  <div class="hidden lg:block">
    <div class="sticky top-24"><%= render "shared/ad_slot", slot: "sidebar" %></div>
  </div>
</div>
