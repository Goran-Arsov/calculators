<%# SEO meta tags %>
<% set_meta_tags(
  title: "Destination Point Calculator - Find Endpoint from Bearing and Distance",
  description: "Calculate the destination latitude and longitude from a start coordinate, initial bearing, and distance using the great-circle formula. Supports km, miles, and nautical miles.",
  canonical: geography_destination_point_url,
  og: {
    title: "Destination Point Calculator - Free Online Tool | Calc Hammer",
    description: "Calculate the destination latitude/longitude from a start point, bearing, and distance.",
    url: geography_destination_point_url,
    type: "website",
    site_name: "Calc Hammer"
  }
) %>

<%= breadcrumb_schema([
  { name: "Home", url: root_url },
  { name: "Geography", url: category_url("geography") },
  { name: "Destination Point Calculator", url: geography_destination_point_url }
]) %>

<%= faq_schema([
  { question: "What is a destination point calculation?", answer: "A destination point calculation, also called the direct problem of geodesy, takes a starting latitude and longitude, an initial compass bearing, and a distance to travel, and returns the latitude and longitude you would arrive at. It is the inverse of the bearing-and-distance calculation, which takes two points and computes the bearing and distance between them. This calculator uses the standard spherical great-circle direct formula with Earth's mean radius of 6371 kilometers." },
  { question: "What is an initial bearing?", answer: "An initial bearing is the compass direction you would face at the very start of a great-circle route. Because great circles curve on a sphere, the bearing along the route changes continuously, which is why we specify the bearing at the starting point. A bearing of 0 degrees means due north, 90 is east, 180 is south, and 270 is west, measured clockwise from north." },
  { question: "How accurate is the calculation?", answer: "This calculator assumes a perfectly spherical Earth with a mean radius of 6371 km, which gives accuracy of about 0.5 percent for most practical distances. For professional surveying, aviation, or maritime navigation, use Vincenty's direct formula or a WGS-84 ellipsoidal solver for higher precision. For casual navigation, hiking, trip planning, and general geographic work the spherical result is more than accurate enough." },
  { question: "What can I use this for?", answer: "Destination point calculations are useful for any task where you have a start, a direction, and a distance and need a precise endpoint: finding the location of a radar contact, projecting a drone flight path, plotting a ship's course, extending a surveying baseline, or building simulations and games that track movement on a geographic map. It is also handy for hikers and pilots who need to know where a straight leg of their route will put them." },
  { question: "Does it handle polar and antimeridian cases?", answer: "Yes. The formula handles routes that cross the equator, the prime meridian, or the international dateline. It also handles routes that pass near or over the poles, although bearings near the poles are more sensitive to small changes because all meridians converge there." }
]) %>

<%= calculator_schema_with_ratings(
  name: "Destination Point Calculator",
  description: "Calculate the destination coordinate from a start point, initial bearing, and distance.",
  url: geography_destination_point_url,
  category: "UtilitiesApplication",
  calculator_slug: "destination-point-calculator"
) %>

<%= render "shared/breadcrumbs", items: [
  { name: "Home", url: root_path },
  { name: "Geography", url: category_path("geography") },
  { name: "Destination Point Calculator" }
] %>

<div class="lg:grid lg:grid-cols-3 lg:gap-8">
  <div class="lg:col-span-2">
    <h1 class="text-3xl md:text-4xl font-extrabold text-gray-900 dark:text-white mb-3 tracking-tight">Destination Point Calculator</h1>
    <p class="text-lg text-gray-500 dark:text-gray-400 mb-8 leading-relaxed">Calculate the destination latitude and longitude from a starting coordinate, an initial bearing, and a travel distance along a great-circle route.</p>

    <div class="mb-8"><%= render "shared/ad_slot", slot: "leaderboard" %></div>

    <div data-controller="destination-point-calculator ad-engagement" class="bg-white dark:bg-gray-900 rounded-2xl shadow-sm border border-gray-200/80 dark:border-gray-800 p-6 md:p-8 mb-8">
      <div class="grid grid-cols-1 md:grid-cols-2 gap-6 md:gap-8">
        <div class="space-y-4">
          <div>
            <label class="block text-sm font-semibold text-gray-600 dark:text-gray-400 mb-1.5">Start Latitude (°)</label>
            <input type="number" inputmode="decimal" data-destination-point-calculator-target="lat" data-action="input->destination-point-calculator#calculate" class="w-full" value="40.7128" step="any" min="-90" max="90">
          </div>
          <div>
            <label class="block text-sm font-semibold text-gray-600 dark:text-gray-400 mb-1.5">Start Longitude (°)</label>
            <input type="number" inputmode="decimal" data-destination-point-calculator-target="lon" data-action="input->destination-point-calculator#calculate" class="w-full" value="-74.0060" step="any" min="-180" max="180">
          </div>
          <div>
            <label class="block text-sm font-semibold text-gray-600 dark:text-gray-400 mb-1.5">Initial Bearing (° from N)</label>
            <input type="number" inputmode="decimal" data-destination-point-calculator-target="bearing" data-action="input->destination-point-calculator#calculate" class="w-full" value="51" step="any" min="0" max="360">
          </div>
          <div>
            <label class="block text-sm font-semibold text-gray-600 dark:text-gray-400 mb-1.5">Distance</label>
            <input type="number" inputmode="decimal" data-destination-point-calculator-target="distance" data-action="input->destination-point-calculator#calculate" class="w-full" value="5570" step="any" min="0">
          </div>
          <div>
            <label class="block text-sm font-semibold text-gray-600 dark:text-gray-400 mb-1.5">Distance Unit</label>
            <select data-destination-point-calculator-target="distanceUnit" data-action="change->destination-point-calculator#calculate" class="w-full">
              <option value="km" selected>Kilometers</option>
              <option value="mi">Miles</option>
              <option value="nmi">Nautical Miles</option>
              <option value="m">Meters</option>
            </select>
          </div>
        </div>
        <div aria-live="polite" aria-atomic="true" class="bg-gradient-to-br from-blue-50 to-indigo-50 dark:from-blue-900/20 dark:to-indigo-900/20 rounded-2xl p-6">
          <h2 class="text-lg font-semibold text-gray-900 dark:text-white mb-5">Destination</h2>
          <div class="space-y-3">
            <div>
              <span class="text-gray-600 dark:text-gray-400 text-sm block">Latitude</span>
              <span class="text-2xl font-extrabold text-blue-600 dark:text-blue-400" data-destination-point-calculator-target="resultLat">0</span>
            </div>
            <div>
              <span class="text-gray-600 dark:text-gray-400 text-sm block">Longitude</span>
              <span class="text-2xl font-extrabold text-blue-600 dark:text-blue-400" data-destination-point-calculator-target="resultLon">0</span>
            </div>
          </div>
          <button data-action="click->destination-point-calculator#copy" class="mt-6 w-full px-4 py-2.5 bg-gradient-to-r from-blue-600 to-indigo-600 hover:from-blue-700 hover:to-indigo-700 text-white rounded-xl shadow-sm shadow-blue-500/20 hover:shadow-md hover:shadow-blue-500/25 transition-all duration-200 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2 text-sm font-medium">Copy Results</button>
        </div>
      </div>
    </div>

    <%= render "shared/calculator_rating", slug: "destination-point-calculator" %>
    <div class="mb-8"><%= render "shared/ad_slot", slot: "in_results" %></div>

    <article class="prose dark:prose-invert max-w-none">
      <h2>How to Use the Destination Point Calculator</h2>
      <p>
        Enter the latitude and longitude of your starting point, the compass bearing you intend to travel, and the distance you want to cover. Choose the distance unit (kilometers, miles, nautical miles, or meters) and the calculator instantly returns the coordinates of the destination. The default example starts in New York City heading roughly northeast at 51 degrees for 5570 kilometers, which lands close to London, demonstrating how the great-circle direct formula matches real-world transatlantic routes.
      </p>

      <div class="my-6"><%= render "shared/ad_slot", slot: "in_content" %></div>

      <h2>The Great-Circle Direct Formula</h2>
      <p>
        The spherical direct formula takes a starting latitude and longitude, an angular distance (distance divided by Earth's radius), and an initial bearing, and returns the destination latitude and longitude. It is a closed-form trigonometric expression that uses the latitude and bearing at the start point, along with the distance to travel. The same formula is used in flight management systems, GIS software, and any application that needs to predict where a moving object will end up given its current position, heading, and speed.
      </p>

      <h2>When to Use This Calculator</h2>
      <p>
        Destination point calculations appear in many real-world problems. Air traffic controllers use them to predict where an aircraft will be at a given time. Radar operators use them to translate a detected range and bearing from a station into a target coordinate. Hikers and sailors use them to project a leg of a route to a waypoint. Drone operators and autonomous vehicle planners use them when building paths. Surveyors extend baselines with them. Game developers use them in world simulations that need to track characters on a real or fictional planet.
      </p>

      <h2>Bearing and Distance Tips</h2>
      <p>
        Remember that a bearing is a true compass direction measured clockwise from geographic north, not from magnetic north. If you have a magnetic bearing from a handheld compass, you must apply your local magnetic declination before using it here. Also remember that great-circle bearings change along the route: the bearing returned by this formula is the initial bearing at the starting point, not the average bearing across the entire journey. For long routes you should re-compute the bearing at intermediate waypoints.
      </p>

      <h2>Frequently Asked Questions</h2>

      <h3>What is a destination point calculation?</h3>
      <p>It takes a starting coordinate, an initial bearing, and a distance, and returns the endpoint along a great-circle route.</p>

      <h3>What is an initial bearing?</h3>
      <p>The compass direction at the start of a great-circle route. Because the bearing changes along the route, we specify the direction only at the starting point.</p>

      <h3>How accurate is the calculation?</h3>
      <p>About 0.5 percent, using a mean Earth radius. For survey-grade work, use Vincenty or a WGS-84 solver.</p>

      <h3>What can I use this for?</h3>
      <p>Air traffic prediction, radar plotting, drone paths, sailing waypoints, surveying, simulations, and games.</p>

      <h3>Does it handle antimeridian crossings?</h3>
      <p>Yes. The formula normalizes longitude correctly even if the great-circle path crosses the international dateline or approaches the poles.</p>

      <%= render "shared/print_button" %>
      <%= render "shared/calculation_email_capture" %>
    </article>

    <%= render "shared/related_calculators", related: [
      { name: "Coordinate Distance Calculator", description: "Calculate the great-circle distance between two latitude/longitude coordinates.", path: geography_coordinate_distance_path, icon_path: "M9 20l-5.447-2.724A1 1 0 013 16.382V5.618a1 1 0 011.447-.894L9 7m0 13l6-3m-6 3V7m6 10l4.553 2.276A1 1 0 0021 18.382V7.618a1 1 0 00-.553-.894L15 4m0 13V4m-6 3l6-3" },
      { name: "Bearing Calculator", description: "Calculate the initial compass bearing between two latitude/longitude points.", path: geography_bearing_path, icon_path: "M9.663 17h4.673M12 3v1m6.364 1.636l-.707.707M21 12h-1M4 12H3m3.343-5.657l-.707-.707m2.828 9.9a5 5 0 117.072 0l-.548.547A3.374 3.374 0 0014 18.469V19a2 2 0 11-4 0v-.531c0-.895-.356-1.754-.988-2.386l-.548-.547z" },
      { name: "Antipode Calculator", description: "Find the point on the opposite side of the Earth from any coordinate.", path: geography_antipode_path, icon_path: "M3.055 11H5a2 2 0 012 2v1a2 2 0 002 2 2 2 0 012 2v2.945M8 3.935V5.5A2.5 2.5 0 0010.5 8h.5a2 2 0 012 2 2 2 0 104 0 2 2 0 012-2h1.064M15 20.488V18a2 2 0 012-2h3.064" }
    ] %>

    <%= render "shared/calculator_disclaimer", category: "geography" %>

    <% blog_posts = related_blog_posts("destination-point-calculator") %>
    <%= render "shared/recommended_reading", posts: blog_posts if blog_posts.any? %>
  </div>

  <div class="hidden lg:block">
    <div class="sticky top-24"><%= render "shared/ad_slot", slot: "sidebar" %></div>
  </div>
</div>
